# 《游戏 V2 技术架构·最新整合版》

> **CozyVale V2 / Pixel Art × 真实三维空间 × 可编辑土地 × Tiny Glade-like 程序化建造 × Kenshi-like 世界物体 × NPC 生活模拟**
>
> **版本：V2.1 Integrated Master**  
> **更新日期：2026-09-11**  
> **引擎：Godot 4.7.2**  
> **目标平台：PC + Mobile**  
> **当前目标：先完成 Vertical Slice，再扩展世界树、村庄、魔域、飞艇与冒险系统**
>
> 本文档整合并更新：
>
> 1. 《游戏 V2 技术架构》
> 2. 《游戏 V2 板块计划》
> 3. 《Tiny Glade-like 程序化建造系统 V1.0 技术设计文档》
>
> 本文件作为后续 Claude Code / 人工开发的**单一技术总纲**。当旧文档与本文发生冲突时，以本文的“锁定决策、数据结构、依赖关系、当前实现状态、Roadmap”章节为准。

---

# 0. 文档定位与当前状态

## 0.1 项目的最终目标

这不是一个“点击按钮盖房子”的游戏，而是：

> **玩家能够改变世界结构，而 Terrain、Building、WorldObject、NPC、生产与导航会对这些改变产生真实反应的 Pixel Art 生活冒险 RPG。**

玩家形成的长期循环：

```text
探索自然世界
    ↓
采集资源 / 改造土地
    ↓
获得可建造区域
    ↓
表达建筑意图
    ↓
程序化生成结构
    ↓
消耗真实材料
    ↓
自由摆放家具与设施
    ↓
NPC 使用空间与设施
    ↓
生产 / 生活 / 储存 / 工作
    ↓
世界状态发生变化
    ↓
玩家继续改造世界
```

## 0.2 当前真实开发状态

2026-09-11 的仓库已经完成并验证了：

- Phase 0 技术验证。
- Phase 1 Spatial Core。
- Room 从 Wall Graph 自动推导。
- Door / Stair Portal 与跨楼层 Room Graph 路由。
- Local Navigation，并验证家具加入后路径会重新绕行。
- Live Building 原型：按 `B` 拖出墙，Room / Portal / Room Graph / Local Navigation 会重新计算。
- Wall Connection Solver。
- Wall Openings：Door / Window 作为 Wall 的参数化开口。
- WorldObject 基础实例与自由放置原型。
- InteractionPoint。
- 简化 Job / Task 与跨楼层工作流程。
- 已经存在一个可运行的 playable slice：NPC 可以找到楼上的 WorkPoint，经过楼梯到达并工作。

当前尚未完成或仍处于基础状态：

- FineGrid + Terrain Chunk。
- Terrain Materials 正式系统。
- Dig / Fill / Polygon Terrain Editing。
- Road Rasterization 正式实现。
- Foundation Solver 正式实现。
- Roof Generator 正式实现。
- Container / Inventory 正式系统。
- 完整 NPC Data Model / Schedule / Needs。
- Save / Load。
- 正式美术资产管线。

因此当前工程不是从零开始，而是已经拥有 **“Spatial → Building → Object → Interaction → NPC” 的骨架验证**，接下来应把 Terrain 与真正程序化建筑闭环补上。

---

# 1. 锁定技术决策

## 1.1 坐标系

文档设计语义使用：

```text
X = 横向
Y = 平面前后
Z = 高度
```

但 Godot 使用 **Y-up**。当前项目锁定映射：

```text
doc(x, y, z) -> Godot(x, z, y)
```

因此：

```text
Doc Floor 0 = Z 0
Doc Floor 1 = Z 3

Godot Floor 0 = Y 0
Godot Floor 1 = Y 3
```

高度永远是真实空间高度，不是 Render Layer。

---

## 1.2 渲染方向

锁定：

> **Real 3D World + Orthographic Camera + 2D Pixel Characters + Pixel-art Surface + Stylized Lighting**

当前约束：

| 层 | 决策 |
|---|---|
| 世界位置 | 真 3D |
| 楼层 | 真 3D 高度 |
| 墙 / 楼板 | 真 3D Mesh + Collider |
| 光照 | 真 3D |
| 摄像机 | Orthographic |
| 角色 | 2D Pixel Sprite / Billboard |
| 表面 | Pixel-art Texture / Nearest |
| 输出基准 | 640×360，再整数缩放 |

核心思想：

> **用 3D 解决空间问题，用 Pixel Art 解决视觉问题。**

---

## 1.3 建筑不是 Prefab，Terrain 也不是 Tile

两个底层原则：

```text
Terrain = Material Field
Building = Structural State
```

不是：

```text
Terrain = 大 Tile 图片
Building = 一栋预制房子
```

---

## 1.4 所有玩家编辑操作都先变成 Intent

任何直接操作 Mesh 的设计都禁止成为正式架构。

统一流程：

```text
Player Input
    ↓
Intent
    ↓
State Mutation
    ↓
Spatial / Rule Analysis
    ↓
Generator
    ↓
Runtime World
```

例如：

```text
DRAW_WALL
MOVE_WALL
DELETE_WALL
CLEAR_TERRAIN
DIG_TERRAIN
FILL_TERRAIN
DRAW_ROAD
PLACE_OBJECT
MOVE_OBJECT
ROTATE_OBJECT
ADD_OPENING
```

---

# 2. 总体架构

```text
GAME
│
├── UI / Input
│
├── WORLD
│   │
│   ├── SpatialSystem
│   │   ├── World Position
│   │   ├── Floor
│   │   ├── Room
│   │   ├── Portal
│   │   ├── Collision
│   │   └── Navigation
│   │
│   ├── TerrainSystem
│   │   ├── TerrainChunk
│   │   ├── FineGrid
│   │   ├── HeightField
│   │   ├── MaterialField
│   │   ├── BuildableMask
│   │   ├── Dig / Fill / Flatten
│   │   ├── Road
│   │   └── Optional Local Material Simulation
│   │
│   ├── BuildingSystem
│   │   ├── BuildingState
│   │   ├── FloorState
│   │   ├── WallGraph
│   │   ├── RoomGraph
│   │   ├── Foundation
│   │   ├── Openings
│   │   ├── Roof
│   │   ├── Stairs
│   │   └── Procedural Generation
│   │
│   ├── ObjectSystem
│   │   ├── WorldObject
│   │   ├── Furniture
│   │   ├── Workstation
│   │   ├── Container
│   │   ├── Machine
│   │   ├── ItemInstance
│   │   └── Placement Solver
│   │
│   ├── InteractionSystem
│   │   ├── InteractionPoint
│   │   ├── Work
│   │   ├── Sit
│   │   ├── Sleep
│   │   ├── Store
│   │   ├── Take
│   │   └── Use
│   │
│   ├── NPCSystem
│   │   ├── NPC
│   │   ├── Job
│   │   ├── Task
│   │   ├── Needs
│   │   ├── Schedule
│   │   └── Decision
│   │
│   └── SimulationSystem
│       ├── Resource
│       ├── Production
│       ├── World State
│       └── Event
│
├── Data
│   ├── Material Definitions
│   ├── Building Definitions
│   ├── Object Definitions
│   ├── Item Definitions
│   ├── Recipe Definitions
│   ├── Job Definitions
│   └── Style Definitions
│
├── SaveSystem
│
└── Renderer
    ├── Orthographic Camera
    ├── Pixel Surface
    ├── Depth
    ├── Occlusion
    ├── Shadow
    └── Interior Visibility
```

---

# 3. 四层核心世界模型

## 3.1 Terrain：土地是材料

Terrain 描述自然世界的可变表面与材料状态。

最小材料集：

```text
GRASS
SOIL
SAND
STONE
WATER
```

第二阶段再扩展：

```text
SNOW
MUD
CLAY
GRAVEL
ASH
```

魔域等特殊世界再扩展：

```text
MAGIC_SOIL
CORRUPTED_GROUND
CRYSTAL
LAVA
```

---

## 3.2 Building：建筑是结构

```text
Building
├── Foundation
├── Floor
├── Wall
├── Opening
│   ├── Door
│   └── Window
├── Column
├── Beam
├── Stair
└── Roof
```

建筑状态是 Source of Truth，Mesh 只是运行时结果。

---

## 3.3 WorldObject：建筑内部的真实物体

```text
WorldObject
├── Furniture
├── Workstation
├── Machine
├── Container
├── Storage
├── Utility
├── Decoration
└── ItemInstance
```

WorldObject 必须拥有：

```text
Transform
Floor
Room
Collision
InteractionPoints
State
DefinitionId
```

---

## 3.4 NPC：空间的使用者

NPC 不应该知道“某个模型是什么”，而应该知道：

```text
我要做什么
↓
需要什么
↓
寻找合适 InteractionPoint
↓
通过 Room Graph + Local Navigation 到达
↓
执行 Interaction
↓
改变 World State
```

---

# 4. TerrainSystem：新架构的地基

## 4.1 为什么现在必须把 Terrain 放到正式核心

旧设计中的 Terrain 已经支持：

```text
FineGrid
Chunk
Material
Dig
Fill
Road
```

新整合设计进一步规定：

> **Terrain 不只是“可以挖的地”，而是 Building 的前置判定系统。**

建筑不能无条件放在世界任何地方。

完整链路变为：

```text
Natural Terrain
    ↓
Terrain Modification
    ↓
Cleared / Prepared
    ↓
Buildable Mask
    ↓
Foundation Validation
    ↓
Building Intent
    ↓
Building
```

---

# 5. TerrainChunk 与多尺度 Terrain

## 5.1 Chunk

世界必须切分 Chunk：

```text
World
├── Chunk_0_0
├── Chunk_0_1
├── Chunk_1_0
└── ...
```

Chunk 是：

- 内存管理单位。
- 更新单位。
- Dirty Region 的边界。
- 保存 / 加载单位。
- Terrain Mesh 重建单位。

---

## 5.2 FineGrid

FineGrid 不是屏幕像素，也不是建筑 Grid Snap。

它只是 Terrain 内部的离散计算单位。

玩家看到的是连续空间：

```text
Vector3 / Polygon / Spline
```

Terrain 内部计算可以是：

```text
Cell[0]
Cell[1]
Cell[2]
...
```

因此两者可以共存：

```text
Continuous World
        │
        ▼
Terrain Rasterization
        │
        ▼
FineGrid
```

---

# 6. TerrainCell 数据结构

推荐：

```gdscript
class_name TerrainCell

var material_id: String
var height: float
var moisture: float
var fertility: float
var temperature: float
var buildability: int
var flags: int
```

V1 初期允许只实现：

```text
material_id
height
buildability
```

其他字段保留扩展接口。

---

# 7. TerrainMaterialDefinition

材料不要写死在 TerrainSystem 中。

示例：

```json
{
  "id": "grass",
  "display_name": "Grass",
  "hardness": 0.1,
  "buildable_default": false,
  "flammable": true,
  "fertility": 0.7,
  "tags": ["natural", "vegetation"]
}
```

另一个：

```json
{
  "id": "soil",
  "display_name": "Soil",
  "hardness": 0.2,
  "buildable_default": true,
  "flammable": false,
  "fertility": 0.9,
  "tags": ["natural", "diggable", "buildable_candidate"]
}
```

---

# 8. TerrainModificationIntent

所有土地修改都使用：

```text
Shape
+
Operation
+
Parameters
+
Target Material
```

统一抽象：

```gdscript
class_name TerrainModificationIntent

var operation: int
var shape_type: int
var points: PackedVector3Array
var radius: float
var depth: float
var material_id: String
var intensity: float
```

操作类型：

```text
CLEAR_VEGETATION
PAINT_MATERIAL
DIG
FILL
FLATTEN
RAISE
LOWER
DRAW_ROAD
```

---

# 9. Polygon Terrain Editing

玩家可以画：

```text
三角形
矩形
多边形
```

例如：

```text
        /\
       /  \
      /    \
     /______\
```

系统流程：

```text
Mouse Input
    ↓
World Raycast
    ↓
Collect Points
    ↓
Polygon
    ↓
Rasterize to Chunk Cells
    ↓
Apply Operation
    ↓
Mark Dirty Cells
    ↓
Rebuild Terrain Surface
    ↓
Recalculate Buildability
```

---

# 10. “清理三角形草坪”正式设计

玩家要实现：

> 清理一块三角形草坪，把它变成土地，然后才能开始建造。

正式流程：

```text
Grass Terrain
      ↓
Player selects Terrain Tool
      ↓
Draw Triangle
      ↓
TerrainModificationIntent(CLEAR)
      ↓
Polygon Rasterization
      ↓
GRASS → SOIL
      ↓
Vegetation instances removed / density reduced
      ↓
Terrain Buildability recompute
      ↓
Buildable = true
```

这里必须注意：

> **Terrain Mesh 只是结果，不保存 Terrain Mesh 本身。**

保存的是：

```text
Cell Material State
Height
Buildability-related facts
```

加载时重新生成。

---

# 11. BuildabilityState

不能只有 `true / false`。

建议：

```text
NATURAL
CLEARED
PREPARED
BUILDABLE
RESTRICTED
OCCUPIED
```

例如：

```text
Grassland
  ↓
CLEARED
  ↓
PREPARED
  ↓
BUILDABLE
```

水面可能是：

```text
WATER
 ↓
RESTRICTED
```

特殊建筑则可以定义自己的例外规则。

---

# 12. BuildingPlacementValidator

建筑放置前必须询问 Terrain：

```gdscript
TerrainSystem.can_support_building(building_intent)
```

检查：

```text
1. 是否在 Buildable 区域？
2. 坡度是否允许？
3. 高度差是否超过阈值？
4. 是否存在水体？
5. 是否与现有 Foundation 冲突？
6. 是否允许当前 Building Definition？
7. 是否需要特殊材料 / Foundation？
```

结果：

```text
VALID
VALID_WITH_FOUNDATION
VALID_WITH_SUPPORT
INVALID
```

---

# 13. FoundationSystem

正式建筑链：

```text
Terrain
   ↓
Foundation Solver
   ↓
Floor
   ↓
Wall
   ↓
Roof
```

如果 Terrain 不平：

```text
Terrain Height Field
        ↓
Foundation Solver
        ↓
Calculate Support Points
        ↓
Choose:
  - Level Foundation
  - Stepped Foundation
  - Column Support
  - Bridge-like Support
```

第一版只需要：

```text
Flat / Slight Slope
```

后续再实现：

```text
Stepped Foundation
Pillars
Raised Platform
```

---

# 14. Terrain 与 Building 的双向影响

不能只考虑：

```text
Terrain → Building
```

未来还要支持：

```text
Building → Terrain
```

例如：

```text
建筑压实土地
道路改变 Grass 密度
地基覆盖 Soil
墙体阻挡某些 Terrain Effect
排水影响 Moisture
```

但 V1 不做完整环境模拟，只保留接口。

---

# 15. Road System

道路采用连续 Path，而不是 Tile 拼接。

流程：

```text
Player Draw Path
    ↓
Control Points
    ↓
Smooth Spline
    ↓
Calculate Width
    ↓
Rasterize
    ↓
Terrain Mask
    ↓
Grass / Soil → Road Material
```

道路视觉通过：

```text
Mask
+
Boundary
+
Material
+
Seeded Detail
```

生成：

- 草边
- 石头
- 泥土
- 车辙
- 小碎石

---

# 16. Noita-like 材料互动：范围必须受控

Noita 式技术概念包括：

```text
Falling-Sand Simulation
Cellular Automata
Material Simulation
Local Rules
```

本项目不在第一阶段复制完整 Noita。

原则：

> **宏观使用 3D Terrain，局部区域才启用材料模拟。**

初期只实现：

```text
Material
+
Basic Height
+
Dig
+
Fill
```

以后局部区域再支持：

```text
Water Flow
Mud
Sand Falling
Fire Spread
Soil Moisture
Magic Corruption
```

Simulation Area 必须局部化：

```text
World
 ↓
Chunk
 ↓
Active Simulation Region
```

不允许一开始对整张大地图进行高频 Cellular Automata 更新。

---

# 17. BuildingSystem 总体原则

BuildingSystem 采用：

> **Tiny Glade-like Intent-driven Procedural Building**

即：

```text
玩家描述“想要什么结构”
        ↓
BuildingState
        ↓
Spatial / Rule Solver
        ↓
Procedural Generator
        ↓
Geometry
```

玩家不是摆放静态墙模型，而是在表达：

```text
“从 A 到 B 画一面墙。”
“围成这个区域。”
“把这里变成一个房间。”
“在这里开一个门。”
“把建筑提高一层。”
```

---

# 18. BuildingState

建议结构：

```gdscript
class_name BuildingState

var id: String
var seed: int
var style_id: String
var floors: Array
var walls: Array
var rooms: Array
var openings: Array
var roofs: Array
var stairs: Array
var foundations: Array
var objects: Array
var version: int
```

这是唯一真相。

运行时：

```text
BuildingState
     ↓
Generator
     ↓
Node3D / Mesh / Collision
```

删除 Mesh 不会删除建筑逻辑。

---

# 19. WallState

```gdscript
class_name WallState

var id: String
var start: Vector3
var end: Vector3
var height: float
var thickness: float
var material_id: String
var style_id: String
var floor_id: String
var node_a: String
var node_b: String
var opening_ids: Array[String]
var seed: int
```

`node_a / node_b` 用来建立 Wall Graph。

---

# 20. WallGraph

Wall Graph 是整个建筑程序化系统的核心拓扑数据。

```text
WallNode
   │
   ├── Wall A
   ├── Wall B
   └── Wall C
```

图结构用于：

- 找连接点。
- 解决 Corner。
- 做平面分割。
- 找闭合区域。
- 推导 Room。
- 触发 Roof 重建。

---

# 21. T-Junction 必须作为正式规则处理

已实现验证的 Bug：

> 墙的一端落在另一面墙的中间，只判断“共享端点”会破坏 Planar Graph。

正式规则：

```text
任何落在线段内部的 Node
        ↓
Split Segment
        ↓
形成真正的 Planar Subdivision
```

这个规则必须保留。

因为它是：

```text
Wall Connection
    ↓
Room Detection
    ↓
Procedural Fusion
```

的基础。

---

# 22. Wall Connection Solver

两个 Wall 在连接处不能出现厚度缺口。

解决方案：

```text
Joint
  ↓
Extend each wall by half thickness
  ↓
Meshes overlap
  ↓
Corner appears solid
```

已验证 corner 从不实心到实心的自动求解。

未来可以扩展：

```text
Miter Join
Bevel Join
Corner Block
Beam Joint
Trim
```

第一版优先稳定，不追求复杂几何。

---

# 23. Wall Generator

输入：

```text
start
end
height
thickness
material
style
seed
openings
```

输出：

```text
Wall Mesh
Collision
Material Assignment
Structural Detail
```

长度完全连续：

```text
3m
5m
8m
13.7m
```

角度也完全连续。

---

# 24. Openings：门窗都是 Wall 属性

不能把 Door 当作一个独立洞口去修改 WallGraph。

正式结构：

```text
Wall
└── Openings
    ├── Door
    └── Window
```

Wall Generator 根据 opening 分割自己的几何体。

### Door

```text
下部：开放
上部：Lintel / Wall
```

### Window

```text
中部：开放
下部：Sill
上部：Wall
```

这样：

- Door 可以通行。
- Window 可以看到 / 透光。
- NPC 不会穿窗。
- Room topology 不需要额外“补边”。

---

# 25. Room Detection

Room 不是玩家手工填写的主结构。

它由：

```text
Floor
+
Planar Wall Graph
```

推导。

流程：

```text
Wall Segments
    ↓
Planar Subdivision
    ↓
Face Traversal
    ↓
Closed Regions
    ↓
RoomState
```

当前已经验证：

```text
8 × 6 = 48 m²
```

房间能够正确生成，并且 T-Junction 修复后，切墙会自动把一个房间变成两个房间。

---

# 26. RoomState

```gdscript
class_name RoomState

var id: String
var floor_id: String
var polygon: PackedVector2Array
var area: float
var portal_ids: Array[String]
var object_ids: Array[String]
var navigation_region_id: String
var room_type: String
```

`room_type` 初期可以允许为空，后续根据家具 / 功能推断：

```text
Bedroom
Kitchen
Workshop
Storage
Laboratory
Hall
```

---

# 27. Door / Stair / Portal

Portal 是空间拓扑连接器。

统一：

```text
Portal
├── id
├── type
├── room_a
├── room_b
├── floor_a
├── floor_b
├── position
└── traversal_rule
```

类型：

```text
Door
Stair
Elevator
Tunnel
CaveEntrance
```

Door：

```text
Room A ↔ Room B
same floor
```

Stair：

```text
Floor 0 / Room A
      ↕
Floor 1 / Room B
```

---

# 28. Room Graph + Local Navigation

采用两级寻路：

```text
Room Graph = “走哪几个空间”
Local Navigation = “具体怎么走”
```

例如：

```text
Outdoors
  ↓ Door
Room 0
  ↓ Stair
Room 1
  ↓
Textile Machine
```

Local Navigation 再负责绕开：

```text
Table
Shelf
Machine
Decoration
```

当前已验证：放置一张 0.6 × 4.0m 的桌子会使路径自动绕行，路径增长约 1.23m。

---

# 29. FloorSystem

Floor 是真正的空间层。

```text
Floor
├── elevation
├── rooms
├── wall_ids
├── objects
├── stairs
└── navigation
```

楼层不是 Sprite Layer，也不是 Z-index。

---

# 30. StairSystem

```text
Stair
├── start_floor
├── end_floor
├── start_position
├── end_position
├── width
└── navigation_connection
```

必须有：

```text
Low-end entry
Landing
Floor-level exit
```

否则会出现：

- 南向碰撞面像墙。
- 上楼没有落点。
- NPC 到达路径节点但实际没到楼上。

这些都已经在当前技术 spike 中实际暴露并修复。

---

# 31. RoofGenerator

屋顶不能使用固定 Prefab 作为逻辑。

输入：

```text
Room Polygon
Wall Graph
Floor Elevation
Roof Style
Material
Seed
```

流程：

```text
Room Polygon
    ↓
Boundary Analysis
    ↓
Ridge / Hip Calculation
    ↓
Generate Roof Geometry
    ↓
Generate Material / Tiles
    ↓
Procedural Details
```

初期支持：

```text
GABLE
FLAT
HIP
```

后期：

```text
MANSARD
TOWER
CURVED
MAGIC
```

---

# 32. Building Dirty Dependency Graph

建筑修改后绝对不能全世界重建。

依赖：

```text
Wall Changed
   ↓
Wall Geometry Dirty
   ↓
Room Detection Dirty
   ↓
Roof Dirty
   ↓
Opening Dirty
   ↓
Navigation Dirty
```

如果只是：

```text
Decoration moved
```

不应该触发：

```text
Roof rebuild
Whole building rebuild
```

---

# 33. Dirty Region

所有编辑都产生 Dirty Region：

```text
DirtyRegion
├── world_bounds
├── chunk_ids
├── affected_walls
├── affected_rooms
├── affected_objects
└── nav_dirty
```

原则：

> **局部修改，局部重建。**

---

# 34. 材料消耗：建筑不再“凭空生成”

这是 V2.1 新增的正式规则。

建造成本必须与几何结构绑定。

例如：

```text
Wall Length = 8m
Height = 3m
Thickness = 0.3m
```

几何体积：

```text
8 × 3 × 0.3 = 7.2 m³
```

Building Definition 再把几何量转换为资源量：

```text
Stone = 720
Wood = 30
Mortar = 50
```

真实代码中不一定使用“1 m³ = 100 units”这样的固定换算，而应由 Material / Building Recipe 定义。

---

# 35. ConstructionMaterial 与 ItemSystem 的关系

不要为建筑材料建立一套孤立库存系统。

统一：

```text
World Item
    ↓
Inventory
    ↓
Construction Cost
```

例如：

```text
stone
wood
clay
fiber
metal
```

既可以：

- 用于建房。
- 放进仓库。
- 交给 NPC。
- 用于生产。
- 作为 Crafting Input。

---

# 36. BuildingDefinition

建筑定义应数据驱动：

```json
{
  "id": "stone_wall",
  "category": "wall",
  "material": "stone",
  "style": "rough",
  "min_height": 2.0,
  "max_height": 6.0,
  "base_cost": {
    "stone": 20,
    "mortar": 4
  },
  "cost_per_m3": {
    "stone": 100,
    "mortar": 12
  },
  "terrain_requirements": [
    "prepared"
  ]
}
```

这样不同材料 / 风格不会污染 BuildingSystem。

---

# 37. WorldObject：Kenshi 式自由摆放

自由摆放使用连续 Transform：

```text
position = (10.34, 7.82, 0)
rotation = 37.5°
```

不默认使用 Tile Snap。

但是：

> **Free Placement ≠ 无规则 Placement。**

必须有 Placement Validation。

---

# 38. Placement Solver

流程：

```text
Mouse
  ↓
Raycast
  ↓
Object Transform
  ↓
Collision Check
  ↓
Support Check
  ↓
Room Check
  ↓
Door / Stair Clearance
  ↓
Navigation Clearance
  ↓
Valid / Invalid
```

辅助吸附：

```text
Wall Snap
Corner Snap
Object Snap
Optional Grid Snap
```

只能作为辅助。

---

# 39. WorldObject Definition / Instance

必须分离：

```text
Definition
    ↓
Instance
```

例如：

```text
Chair Definition
      ↓
Chair_001
Chair_002
Chair_003
```

Instance 只保存：

```text
definition_id
position
rotation
floor_id
room_id
state
```

---

# 40. InteractionPoint

核心结构：

```gdscript
class_name InteractionPoint

var id: String
var type: String
var local_position: Vector3
var local_rotation: Basis
var capacity: int
var requirements: Dictionary
var action_id: String
```

类型：

```text
WORK
SIT
SLEEP
STORE
TAKE
PUT
FUEL
MATERIAL
OUTPUT
REPAIR
TALK
```

NPC 永远先找 InteractionPoint，再找 Object。

---

# 41. Interaction

统一：

```text
Interaction
├── actor
├── target
├── point
├── action
├── requirements
├── duration
└── result
```

例如：

```text
NPC_001
ResearchTable_001
WorkPoint
Research
30 sec
```

---

# 42. Workstation / Container / Furniture

### Workstation

```text
ResearchTable
TextileMachine
Forge
Kitchen
AlchemyTable
```

### Container

```text
Chest
Crate
Cabinet
WarehouseBox
```

### Furniture

```text
Chair
Bed
Table
Bench
Shelf
```

一个对象可以同时拥有多个 InteractionPoint。

例如 TextileMachine：

```text
MaterialPoint
WorkPoint
OutputPoint
StoragePoint
```

---

# 43. NPCSystem

NPC 数据结构：

```text
NPC
├── identity
├── transform
├── attributes
├── skills
├── needs
├── inventory
├── job
├── schedule
├── current_task
└── state
```

Skill 例如：

```text
Research
Farming
Mining
Cooking
Textile
Building
Combat
Medicine
Trading
```

---

# 44. Job 与 Task

必须区分：

```text
Job = 我负责什么
Task = 我现在具体做什么
```

例如：

```text
Job = Researcher
```

当前 Task：

```text
FindInput
→
GoToStorage
→
TakeItem
→
GoToWorkPoint
→
Research
→
PutOutput
```

---

# 45. NPC 生产工作链

完整生产工作链：

```text
Job
 ↓
Need / Recipe
 ↓
Find Input
 ↓
Find Container
 ↓
Navigate
 ↓
Take
 ↓
Navigate to WorkPoint
 ↓
Work
 ↓
Produce Output
 ↓
Store
```

NPC 不应该因为增加一个新工作台而增加一段特殊硬编码。

---

# 46. Data-Driven Design

统一原则：

```text
Definition
   ↓
Instance
   ↓
System
```

数据分类：

```text
items/
objects/
materials/
buildings/
recipes/
jobs/
styles/
```

示例：

```text
objects/research_table.json
recipes/cloth.json
jobs/researcher.json
materials/stone.json
buildings/stone_wall.json
```

添加新内容的理想操作是：

> **增加数据，而不是修改核心系统。**

---

# 47. Procedural Art System：美术到底怎么做

核心原则：

> **不要为每一栋房子制作一个完整模型。**

采用三层：

```text
Base Assets
     ↓
Procedural Assembly
     ↓
Procedural Detail
```

---

# 48. Base Assets

真正由美术师制作的是“视觉原子”：

### 石材

```text
StoneBlock
StoneCorner
StoneCap
StoneColumn
StoneTrim
```

### 木材

```text
WoodBeam
WoodPlank
Log
Frame
```

### 屋顶

```text
RoofTile
Ridge
RoofCap
```

### 门窗

```text
Door
Window
Frame
Sill
```

### 植物

```text
Grass
Flower
Bush
Tree
Vine
Moss
```

### 家具

```text
Chair
Table
Bed
Chest
Workbench
Shelf
Lantern
```

这些不是最终建筑，而是构件库。

---

# 49. Procedural Assembly

例如一堵 Stone Wall：

```text
WallState
   ↓
Calculate Length / Height / Thickness
   ↓
Select Stone Blocks
   ↓
Arrange Blocks
   ↓
Solve Corner
   ↓
Insert Beam / Trim
   ↓
Apply Material
```

因此 4m / 8m / 12m 的墙可以保持统一风格而不必准备 3 套完整模型。

---

# 50. Procedural Detail

最后加入：

```text
Moss
Cracks
Grass
Small Stones
Vines
Lanterns
Wood Wear
Color Variation
```

但这些都必须是：

> **确定性随机（Deterministic Random）。**

---

# 51. Art Grammar

整个游戏建立一套“视觉语法”。

例如：

```text
StoneStyle
WoodStyle
GrassStyle
VegetationStyle
RoofStyle
DecorationStyle
LightingStyle
```

所有生成器都调用这些风格规则。

因此：

```text
Wall
Road
Bridge
Tower
Castle
Floor
```

虽然不是同一个模型，却仍然像来自同一个世界。

---

# 52. 统一美术风格的五个控制维度

程序化美术的统一性来自：

```text
1. 材质语言
2. 比例语言
3. 色彩语言
4. 细节尺度
5. 光照语言
```

而不是来自“所有模型长得一样”。

可以允许：

```text
结构随机
砖块随机
装饰随机
植物随机
```

但必须统一：

```text
Palette
Roughness Range
Detail Scale
Edge Treatment
Lighting
```

一句话：

> **随机的是结构，统一的是语言。**

---

# 53. Seed System

每一个 Building、Room、Wall、Chunk 都可以拥有 Seed。

例如：

```text
BuildingSeed = Hash(BuildingId)
WallSeed = Hash(BuildingSeed, WallId)
DetailSeed = Hash(WallSeed, DetailType)
```

同一个 State：

```text
第一次生成
=
重新生成
=
Save / Load 后生成
```

这样程序化美术不会因为重建而突然改变。

---

# 54. Art / Logic 解耦

游戏逻辑不能依赖具体 Mesh 文件。

Roadmap 当前已经采用了一个正确原则：

```text
pixel_art.gd
materials.gd
```

属于替换视觉资产的边界。

正式版本继续扩展成：

```text
Style Definition
Material Definition
Mesh Library
Texture Library
Generator Rules
```

这样后期换画风不会重写：

```text
TerrainSystem
BuildingSystem
NPCSystem
```

---

# 55. Renderer 架构

```text
World State
    ↓
Generated Geometry
    ↓
Renderer
    ↓
Orthographic Camera
    ↓
Depth / Occlusion
    ↓
Pixel Surface
    ↓
Stylized Lighting
    ↓
640×360
    ↓
Integer Scale
```

---

# 56. Character Renderer

角色采用：

```text
Real 3D Position
+
2D Pixel Sprite
```

角色位置仍然由：

```text
X / Y / Z
```

决定。

但显示为：

```text
2D Pixel Sprite / Billboard
```

因此 NPC：

- 可以站在楼上。
- 可以走过楼梯。
- 可以被墙遮挡。
- 可以进入房间。

而不需要制作高精度 3D 角色。

---

# 57. Occlusion / Interior Visibility

需要解决：

```text
玩家进入房屋
↓
墙 / 屋顶遮挡
```

建议：

```text
Camera → Player Raycast
        ↓
Blocking Geometry
        ↓
Fade only blocking elements
```

而不是整栋建筑直接消失。

规则：

```text
Roof Fade
Wall Fade
Room Reveal
```

当前技术 spike 已验证 Occlusion Raycast。

---


# 58. Art Pipeline：无冕之乡统一生成式美术体系

## 58.1 重要说明：当前“无冕之乡”并没有锁死最终美术资产生产方式

当前 V2 工程中的 Art Policy 明确规定：

- 美术暂时使用 placeholder；
- 纹理由运行时程序生成；
- 真实 Pixel Art 素材未来接入时，原则上只需要替换 `render/pixel_art.gd` 与 `data/materials.gd`，不应修改核心游戏逻辑。

因此本章的目标不是声称“当前已经有一套最终美术资源”，而是为 **无冕之乡** 建立一套可以从现在开始执行、并且不会破坏 V2 架构的最终 Art Pipeline。

> **原则：美术资源可以为空，系统不能依赖资源本身才能运行。**

---

## 58.2 我们真正要模仿的不是某一张图，而是一套“视觉生成方式”

本项目希望获得的效果不是：

```text
每一个草
每一块石头
每一栋房子
每一个特效
都人工做一个最终模型
```

而是：

```text
少量高质量基础资产
        ↓
统一材质 / 色板 / 比例 / 细节规则
        ↓
程序化组合
        ↓
程序化变体
        ↓
环境散布
        ↓
最终统一画面
```

因此我们的目标是建立：

> **Base Assets + Procedural Assembly + Procedural Distribution + Procedural VFX + Shared Art Grammar**

这与本项目当前“真实 3D 空间 + Orthographic Camera + Pixel Art Surface”的总路线是一致的。

---

## 58.3 五层美术架构

所有游戏美术分为五层：

```text
Layer 1  Art Grammar
    ↓
Layer 2  Base Assets
    ↓
Layer 3  Procedural Assembly
    ↓
Layer 4  Procedural Distribution / VFX
    ↓
Layer 5  Renderer / Pixel Treatment
```

### Layer 1：Art Grammar

定义整个游戏“长什么样”。

```text
Color Palette
Value Range
Saturation Range
Silhouette Language
Edge Language
Material Language
Detail Scale
Lighting Language
VFX Language
```

### Layer 2：Base Assets

艺术家或 AI 真正需要生产的原子资产。

例如：

```text
GrassBlade_A
GrassTuft_A
Flower_A
Rock_A
Pebble_A
TreeTrunk_A
LeafCluster_A
StoneBlock_A
WoodPlank_A
RoofTile_A
Lantern_A
```

### Layer 3：Procedural Assembly

把原子资产组合成：

```text
Ground
Bush
Wall
Roof
House
Road
Bridge
Tree
Garden
```

### Layer 4：Procedural Distribution / VFX

决定它们：

```text
出现在哪里
出现多少
怎么变化
怎么运动
怎么响应环境
```

### Layer 5：Renderer

统一做：

```text
Orthographic
Pixelation
Nearest Sampling
Depth
Occlusion
Lighting
Shadow
VFX Composite
```

---

## 58.4 地面植物不要制作成“每一根草的模型”

这是整个地表美术系统最关键的原则之一。

不建议：

```text
Grass_0001.glb
Grass_0002.glb
Grass_0003.glb
...
```

而建议建立非常少的植物原子：

```text
GrassBlade
GrassTuft
SmallFlower
LeafCluster
TinyFern
MossPatch
Pebble
SmallRock
```

然后由 `VegetationScatterSystem` 负责组合。

例如：

```text
GrassField
  ↓
Density Map
  ↓
Biome Rule
  ↓
Slope Mask
  ↓
Moisture Mask
  ↓
Road Mask
  ↓
Building Mask
  ↓
Seeded Scatter
  ↓
Grass / Flowers / Rocks / Ferns
```

因此玩家清理土地：

```text
Grass
  ↓
Terrain Modification
  ↓
Grass Density = 0
  ↓
Grass instances disappear
  ↓
Soil becomes visible
```

建筑建成后：

```text
Foundation Mask
  ↓
禁止植被生成
```

道路附近：

```text
Road Mask
  ↓
降低草密度
  ↓
增加碎石 / 草边
```

这比做大量不同地面模型更符合程序化世界的方向。

---

## 58.5 Vegetation Scatter 的核心参数

每个 Chunk 可以有：

```text
VegetationField
├── density
├── biome
├── moisture
├── fertility
├── slope
├── altitude
├── season
├── road_mask
├── building_mask
├── player_clear_mask
└── seed
```

随后每种植物 Definition 提供：

```text
PlantDefinition
├── id
├── prefab / sprite / mesh
├── min_scale
├── max_scale
├── min_density
├── max_density
├── preferred_biomes
├── slope_range
├── moisture_range
├── color_variants
└── rarity
```

这样：

```text
草
花
灌木
蘑菇
蕨类
小石头
```

实际上都可以走同一个 Scatter System。

---

## 58.6 地面不是“一个材质”，而是一套 Material Stack

例如 Grass Terrain：

```text
Base Soil
    ↓
Grass Albedo
    ↓
Grass Color Variation
    ↓
Small Noise
    ↓
Grass Tufts
    ↓
Flowers
    ↓
Rocks
    ↓
Moss / Dirt Patches
```

Road：

```text
Soil
    ↓
Road Mask
    ↓
Packed Dirt
    ↓
Pebbles
    ↓
Wheel / Foot Marks
    ↓
Grass Edge
```

Forest：

```text
Grass / Soil
    ↓
Leaf Litter
    ↓
Moss
    ↓
Small Plants
    ↓
Rocks
    ↓
Tree Scatter
```

因此 Terrain Visual System 与 Terrain State System 分离：

```text
Terrain State
     ↓
Material / Height / Mask
     ↓
Terrain Visual Generator
     ↓
Final Ground
```

---

## 58.7 植物的“统一风格”从哪里来

不能靠每次让 AI“随便生成一棵好看的树”。

必须固定：

```text
Silhouette
Color Family
Highlight Direction
Shadow Value
Outline Policy
Pixel Density
Leaf Cluster Scale
Trunk Proportion
```

例如所有树都遵守：

```text
粗树干
大块叶簇
低频细节
高对比剪影
有限色板
轻微不对称
```

这样即使有几十种树，仍然会像同一个游戏里的树。

---

## 58.8 推荐的植物资产生产方式

### 路线 A：AI 概念图 → 手工整理 → 2D Pixel Asset

适合：

```text
Grass
Flower
Bush
Tree
Vine
Mushroom
Small Prop
```

流程：

```text
AI Generate
    ↓
挑选方向
    ↓
统一色板
    ↓
Pixelorama / Aseprite 清理
    ↓
导出 Sprite / Atlas
    ↓
Godot Scatter
```

Pixelorama 是免费开源的像素画工具，支持像素绘制、动画、Spritesheet，并提供 Windows / Linux / macOS / Web 版本，适合作为本项目的资产清理工具。citeturn614904search3turn614904search15

---

### 路线 B：AI 概念图 → Image-to-3D → 简化 → Pixel Rendering

适合：

```text
Rock
TreeTrunk
Statue
Furniture
Machine
Decoration
```

Meshy 当前支持从图片或文本生成 3D 资产，并支持重新网格化、纹理与多种常见格式导出，可作为“概念图 → 3D 原子资产”的快速原型工具。citeturn996770search0turn996770search3

但是 AI 生成的 3D 模型不能直接等同于最终游戏资产。

建议：

```text
AI Model
 ↓
Remesh / Simplify
 ↓
Scale Normalize
 ↓
Silhouette Cleanup
 ↓
Material Cleanup
 ↓
Godot
```

---

### 路线 C：程序化材质

对于：

```text
Grass
Soil
Stone
Wood
Brick
Roof
Sand
Mud
```

应该优先考虑程序化材质，而不是每一种尺寸都生成一张不同贴图。

Material Maker 是开源的节点式程序化材质工具，可以用节点生成和组合纹理，并导出给 Godot / Unity / Unreal 使用。citeturn614904search2turn614904search14

---

### 路线 D：AI 图像 → Material

如果已经有一张非常喜欢的参考图，可以：

```text
Reference Image
      ↓
Image-to-Material
      ↓
Base Color
Normal
Height
Roughness
AO
      ↓
Material Cleanup
      ↓
Godot
```

Adobe Substance 3D Sampler 当前提供 Image-to-Material，可利用 AI 从单张图像生成 Normal、Height、Roughness 等材质通道；其 Generative Workflow 还提供 Text-to-Texture 与 Image-to-Texture。citeturn614904search0turn614904search6

---

## 58.9 AI 工具不要负责“最终统一画风”，而应该负责“产出原料”

这是本项目 AI 美术使用的最高原则。

错误：

```text
每个资产
→ 单独 Prompt
→ 单独生成
→ 直接进游戏
```

正确：

```text
建立 Art Bible
       ↓
固定视觉规则
       ↓
AI 生成候选资产
       ↓
统一后处理
       ↓
导入 Asset Library
       ↓
程序化组合
       ↓
Game Renderer
```

因此 AI 是：

> **Asset Generator / Concept Generator**

而不是：

> **Visual Director**。

视觉导演必须是我们的 Art Grammar。

---

## 58.10 推荐建立自己的 Asset Bible

建议创建：

```text
art/
└── ART_BIBLE.md
```

内容至少包括：

```text
1. Camera
2. Pixel Resolution
3. Color Palette
4. Lighting
5. Shadow
6. Material
7. Outline
8. Vegetation
9. Architecture
10. Props
11. Characters
12. VFX
13. UI
```

例如：

```text
Pixel Output: 640×360
Sampling: Nearest
Camera: Orthographic

Palette:
- Grass Dark
- Grass Mid
- Grass Light
- Soil Dark
- Soil Mid
- Soil Light

Lighting:
- Soft sunlight
- Warm key light
- Cool ambient shadow

Geometry:
- Soft stylized shapes
- Limited micro-detail
- Strong readable silhouettes
```

真正的数值应在制作第一批测试素材后锁定，不在这里提前拍脑袋决定。

---

## 58.11 AI 提示词模板：地表植物

AI 不建议直接使用“给我一棵漂亮的草”。

推荐模板：

```text
stylized fantasy game vegetation asset,
cozy hand-crafted diorama aesthetic,
soft rounded silhouette,
limited color palette,
clean readable shapes,
slightly irregular natural form,
subtle painterly pixel-art surface,
low-detail but high readability,
soft warm highlights,
cool soft shadows,
isolated single asset,
centered composition,
plain neutral background,
no text,
no UI,
no character,
no photorealism
```

### 草丛

```text
stylized fantasy grass tuft,
cozy hand-crafted diorama game aesthetic,
short layered blades,
rounded silhouette,
3 to 5 green tones,
slightly asymmetrical natural shape,
pixel-art compatible,
clean isolated asset,
neutral background,
no pot,
no ground plane,
no text
```

### 花朵

```text
stylized tiny fantasy wildflower,
cozy handcrafted game asset,
small readable silhouette,
soft rounded leaves,
limited pastel palette,
subtle pixel-art texture,
slightly irregular handmade shape,
isolated on neutral background,
no vase,
no character,
no text
```

### 石头

```text
stylized fantasy field rock,
hand-crafted diorama aesthetic,
soft irregular silhouette,
low-poly inspired shape,
limited earthy palette,
subtle surface variation,
clean readable form,
pixel-art compatible,
isolated single asset,
neutral background,
no grass attached,
no text
```

---

## 58.12 AI 提示词模板：建筑材料

### 石墙砖块

```text
stylized medieval fantasy stone building material,
hand-crafted diorama game aesthetic,
soft chunky stone blocks,
irregular but controlled shapes,
limited muted earthy palette,
subtle moss accents,
clean silhouette,
low visual noise,
pixel-art compatible,
seamless material reference,
neutral lighting,
no modern architecture,
no photorealism
```

### 木板

```text
stylized fantasy wooden plank material,
cozy handcrafted game aesthetic,
chunky simple wooden boards,
subtle grain,
soft bevels,
limited warm brown palette,
slightly uneven handmade construction,
pixel-art compatible,
seamless texture,
no metal,
no photorealism
```

---

## 58.13 AI 提示词模板：VFX

特效最好不要让 AI 直接生成“游戏里的最终实时特效”。

AI 更适合产生：

```text
VFX concept
sprite sheet reference
shape language
color palette
animation keyframes
```

例如火焰：

```text
stylized fantasy game VFX,
hand-crafted magical campfire flame,
chunky readable silhouette,
small number of shapes,
warm orange yellow core,
soft red outer flame,
cozy pixel-art aesthetic,
sprite sheet reference,
transparent background,
multiple animation frames,
no smoke text,
no UI
```

魔法：

```text
stylized fantasy magic particle effect,
cozy pixel-art RPG aesthetic,
small glowing fragments,
soft circular particles,
limited cyan and violet palette,
clear silhouette,
readable at 640x360,
sprite sheet,
transparent background,
no character,
no UI
```

最终应由 Godot 的：

```text
GPUParticles
ParticleProcessMaterial
AnimatedTexture / Sprite Sheet
Shader
Trail
Light
```

组合，而不是把 AI 视频直接放进游戏。

---

## 58.14 VFX 也应该建立 Art Grammar

例如：

### 火焰

```text
Warm
Organic
Upward
Soft
```

### 水

```text
Cool
Flowing
Transparent
Soft
```

### 魔法

```text
High saturation
Glow
Geometric particles
Controlled motion
```

### 魔域

```text
Desaturated base
Purple / black accent
Irregular particles
Slow distortion
```

这样以后几十个 VFX 仍然属于同一个游戏。

---

## 58.15 AI 资产一致性：Reference First

AI 生成游戏资产时，推荐优先使用：

```text
Reference Image
        ↓
Style Reference
        ↓
Prompt
        ↓
Generate Variants
```

而不是纯文字随机生成。

例如可以先制作一张：

```text
MASTER STYLE SHEET
```

里面包含：

```text
草
花
树
石头
木箱
墙
门
灯
```

然后后续资产全部参考这张 Style Sheet。

当前一些 AI 工具已经提供 Reference / Style Reference 能力。例如 Ideogram 支持上传多张参考图来约束生成风格；Scenario 也主打基于自有数据集训练和保持游戏资产的一致视觉风格。citeturn996770search11turn996770search12turn614904search10

---

## 58.16 推荐的无冕之乡完整美术流水线

推荐最终采用：

```text
                 ART BIBLE
                     ↓
               MASTER STYLE
                     ↓
        ┌────────────┼────────────┐
        ↓            ↓            ↓
      2D AI          3D AI       Procedural
        ↓            ↓            ↓
    Sprite        Base Mesh      Material
        ↓            ↓            ↓
      Pixel         Cleanup       Graph
        └────────────┼────────────┘
                     ↓
                Asset Library
                     ↓
        ┌────────────┼────────────┐
        ↓            ↓            ↓
  Building Gen  Terrain Gen   VFX System
        ↓            ↓            ↓
        └────────────┼────────────┘
                     ↓
             Godot Renderer
                     ↓
        Orthographic + Pixel Treatment
                     ↓
                   GAME
```

---

## 58.17 建议的工具组合

### 方案 A：低成本 / 尽量免费

```text
Pixelorama
    ↓
Material Maker
    ↓
Blender
    ↓
Godot
```

Pixelorama 和 Material Maker 都适合本项目的像素资产和程序化材质生产；Material Maker 本身就是基于节点的程序化材质工具。citeturn614904search2turn614904search3

### 方案 B：AI 为主

```text
Scenario / 图像生成工具
        ↓
Pixelorama / Krita 清理
        ↓
Meshy
        ↓
Blender
        ↓
Godot
```

Scenario 当前支持游戏资产、环境、纹理等生成，并支持训练自定义模型以保持视觉一致性；Meshy 可把概念图快速转成可导出的 3D 原子资产。citeturn614904search10turn996770search3

### 方案 C：材质质量优先

```text
AI / Photo Reference
        ↓
Substance 3D Sampler
        ↓
Material Library
        ↓
Godot
```

适合：

```text
Stone
Wood
Soil
Brick
Roof
Metal
Magic Surface
```

Sampler 目前支持 AI Image-to-Material 和生成式 Text-to-Texture / Image-to-Texture，可以作为材质生产链的一环。citeturn614904search0turn614904search6

---

## 58.18 美术资源现在可以全部留空

这是项目必须明确写入架构的一条规则：

```text
NO FINAL ART REQUIRED FOR CORE SYSTEM
```

第一阶段可以只有：

```text
白色墙
简单色块地面
程序生成草
简单粒子
占位家具
```

只要数据接口正确：

```text
Asset Definition
Material Definition
Plant Definition
VFX Definition
```

未来替换：

```text
Placeholder
   ↓
Real Art
```

不能影响：

```text
Terrain
Building
Object
Interaction
NPC
Save
Navigation
```

---

## 58.19 Asset Definition 必须数据驱动

例如：

```gdscript
class_name AssetDefinition

var id: String
var category: String
var mesh_path: String
var texture_path: String
var material_id: String
var style_tags: Array[String]
var scale_range: Vector2
var rotation_mode: String
var lod: Array
```

植物：

```gdscript
class_name PlantDefinition

var id: String
var asset_ids: Array[String]
var biome_tags: Array[String]
var density: float
var scale_range: Vector2
var color_variants: Array[Color]
var slope_range: Vector2
var moisture_range: Vector2
```

VFX：

```gdscript
class_name VFXDefinition

var id: String
var sprite_sheet: String
var palette_id: String
var lifetime: float
var emission_rate: float
var scale_range: Vector2
var color_curve: Gradient
var motion_mode: String
```

这样 AI 换了、模型换了、贴图换了，核心逻辑不变。

---

## 58.20 Art Asset 状态机

每一种资源可以有：

```text
Concept
 ↓
Generated
 ↓
Selected
 ↓
Cleaned
 ↓
Imported
 ↓
Validated
 ↓
Production
```

只有进入：

```text
Production
```

的资源才允许进入正式游戏资产库。

---

## 58.21 美术验收标准

每一个正式资产至少检查：

```text
□ 是否符合 Art Bible？
□ 是否符合比例？
□ 是否符合色板？
□ 是否符合 Pixel Density？
□ 是否存在明显 AI 伪影？
□ 是否有透明背景问题？
□ 是否需要人工修正？
□ 是否能与其他资产放在一起？
□ 是否能在 640×360 下读出来？
□ 是否影响性能？
```

---

## 58.22 最终原则

无冕之乡不应该成为：

> “AI 帮我们生成很多漂亮图片的游戏。”

而应该成为：

> **“AI 帮我们生产视觉原子，程序化系统负责把这些原子组织成一个统一、可无限变化的世界。”**

最终目标：

```text
AI
↓
Assets
↓
Art Grammar
↓
Procedural Generation
↓
Terrain / Building / Object / VFX
↓
Unified World
```

这与 V2 原来的核心原则完全一致：

```text
Data
↓
Rules
↓
Geometry / Simulation
↓
Renderer
```

只是现在正式补上了：

```text
Art Assets
↓
Art Grammar
```

从而形成完整闭环。

---

# 59. Performance：Chunk Everything

性能不能等项目大了再解决。

分块：

```text
Terrain → Terrain Chunk
Building → Spatial Partition / Building Chunk
Object → Spatial Partition
NPC → Spatial Partition
```

玩家附近：

```text
Full Fidelity
```

远处：

```text
Low Activity
```

很远：

```text
Unloaded / Abstracted
```

---

# 60. NPC Simulation LOD

```text
LOD 0
玩家附近
完整 Navigation / Interaction / Animation / Needs

LOD 1
附近区域
低频 AI

LOD 2
较远区域
抽象模拟

LOD 3
非常远
只更新世界结果
```

例如远处 NPC 只需要：

```text
08:00 Work
12:00 Eat
18:00 Home
```

不需要每帧寻找下一步。

---

# 61. Terrain Simulation LOD

同样：

```text
Near
→ Fine Grid + optional material simulation

Far
→ Simplified Material / Height

Very Far
→ Low Resolution Representation
```

Noita-like Simulation 必须只在 Active Simulation Region 高精度运行。

---

# 62. Renderer 性能

禁止默认采用：

```text
10000 Objects
=
10000 expensive independent draw calls
```

优先：

```text
Batching
Instancing
Chunk Mesh
Visibility Culling
LOD
```

Mobile 优先控制：

```text
CPU
GPU
RAM
Draw Call
Shader Complexity
```

---

# 63. SaveSystem

Save 保存：

> **世界事实。**

不保存：

> **可以重新计算的瞬时结果。**

---

# 64. Terrain Save

保存：

```text
ChunkId
MaterialField
HeightField
必要的世界状态
```

不保存：

```text
Terrain Mesh
Runtime Rendering Cache
```

加载：

```text
Save Data
 ↓
TerrainSystem
 ↓
Rebuild Terrain
 ↓
Renderer
```

---

# 65. Building Save

保存：

```text
BuildingId
Seed
Style
Floors
Walls
Rooms (if cached / reconstructible policy allows)
Openings
Roofs (definition, not final mesh)
Stairs
Foundations
```

最严格的 Source of Truth 是：

```text
Building State
```

Mesh 永远重新生成。

---

# 66. Object Save

```text
definition_id
instance_id
position
rotation
floor_id
room_id
state
inventory
```

例如：

```json
{
  "definition_id": "research_table",
  "instance_id": "research_table_001",
  "position": [12.4, 3.0, 8.7],
  "rotation": 90.0,
  "floor_id": "floor_0",
  "room_id": "laboratory",
  "state": {}
}
```

---

# 67. NPC Save

保存：

```text
Identity
Position
Floor
Inventory
Skills
Needs
Job
Schedule
Current Task State
```

不一定保存：

```text
Exact current path
Exact animation
Temporary target
Runtime steering state
```

重载后重新规划。

---

# 68. Save 原则

```text
保存：World Facts

不保存：Derived Cache

不保存：Render Mesh

不保存：Temporary Path
```

这是未来修复 Bug、升级系统和降低存档体积的关键。

---

# 69. Project Directory

推荐正式目录：

```text
res://
├── game/
│   ├── world/
│   │   ├── spatial/
│   │   ├── terrain/
│   │   ├── chunks/
│   │   ├── navigation/
│   │   └── world_state/
│   │
│   ├── building/
│   │   ├── core/
│   │   ├── wall/
│   │   ├── room/
│   │   ├── foundation/
│   │   ├── openings/
│   │   ├── roof/
│   │   ├── stairs/
│   │   ├── rules/
│   │   └── generation/
│   │
│   ├── objects/
│   │   ├── core/
│   │   ├── furniture/
│   │   ├── workstation/
│   │   ├── container/
│   │   ├── production/
│   │   ├── decoration/
│   │   └── placement/
│   │
│   ├── interaction/
│   ├── actors/
│   │   ├── player/
│   │   ├── npc/
│   │   └── mounts/
│   │
│   ├── simulation/
│   │   ├── jobs/
│   │   ├── tasks/
│   │   ├── needs/
│   │   ├── production/
│   │   └── resources/
│   │
│   ├── render/
│   │   ├── camera/
│   │   ├── pixel_art/
│   │   ├── occlusion/
│   │   ├── lighting/
│   │   └── materials/
│   │
│   └── save/
│
├── data/
│   ├── materials/
│   ├── buildings/
│   ├── objects/
│   ├── items/
│   ├── recipes/
│   ├── jobs/
│   └── styles/
│
├── assets/
│   ├── textures/
│   ├── meshes/
│   ├── sprites/
│   ├── materials/
│   └── audio/
│
└── ui/
```

---

# 70. Core Class Map

```text
WorldManager
ChunkManager
SpatialSystem
TerrainManager
TerrainChunk
TerrainCell
TerrainMaterialLibrary
TerrainModifier
BuildabilitySolver
FoundationSolver

BuildingManager
BuildingState
BuildingDefinition
BuildingRuleEngine
WallState
WallGraph
WallGenerator
RoomState
RoomDetector
FloorState
RoofGenerator
OpeningGenerator
PortalManager

WorldObjectManager
WorldObjectState
ObjectDefinition
PlacementSolver
InteractionPoint
InteractionManager
ContainerManager
Inventory

NPCManager
NPCState
JobDefinition
Task
TaskPlanner
NeedsSystem
ScheduleSystem
DecisionSystem

NavigationManager
RoomGraph
LocalNavigation

SaveManager
SeedManager
DefinitionRegistry
StyleRegistry
```

---

# 71. System Dependency Rules

严格遵守：

```text
UI
 ↓
Intent
 ↓
State
 ↓
Rules / Solver
 ↓
Generator
 ↓
Runtime Nodes
```

不能反过来：

```text
UI → Mesh
NPC → Mesh
NPC → Furniture Scene
Building → UI state
```

---

# 72. Vertical Slice：最终目标

原技术文档定义的小型 Vertical Slice 继续保留。

场景可控制在约：

```text
100m × 100m
```

内容：

```text
一片草地
一块可改造土地
一栋两层房子
一个玩家
一个 NPC
一个仓库
一个研究桌
一个纺织机
一套楼梯
少量家具
```

但 V2.1 应把验证路径更新成：

```text
进入自然草地
 ↓
画三角形
 ↓
Grass → Soil
 ↓
Buildable Mask 出现
 ↓
检查 Foundation
 ↓
消耗材料
 ↓
Draw Wall
 ↓
自动连接
 ↓
自动 Room
 ↓
Door / Window
 ↓
Roof
 ↓
第二层
 ↓
放家具
 ↓
NPC 使用
 ↓
Save / Load
```

---

# 73. 第一 Vertical Slice 真实验收标准

## Terrain

```text
□ 可以选择自然土地
□ 可以画三角形 / 多边形
□ Grass 可以变 Soil
□ Terrain Mask 正确重建
□ Buildable Mask 正确更新
□ 不允许建筑时有明确原因
□ Terrain 修改不会全图重建
```

## Building

```text
□ 墙可以任意长度
□ 墙可以任意角度
□ 墙连接处没有厚度缺口
□ T-junction 正确分割
□ Room 自动生成
□ Door / Window 是 Wall Openings
□ Roof 根据 Room Polygon 自动生成
□ Foundation 根据 Terrain 检查
□ 建造消耗材料
```

## Object

```text
□ 家具可以自由摆放
□ 家具可旋转
□ Collision 正确
□ Placement Validation 正确
□ 家具影响 Navigation
```

## Interaction

```text
□ Object 可以提供 InteractionPoint
□ NPC 可以寻找 WorkPoint
□ NPC 可以 Sit / Sleep / Store / Work
```

## NPC

```text
□ NPC 有 Job
□ Job 能产生 Task
□ Task 能寻找资源
□ Task 能寻找 InteractionPoint
□ NPC 可以跨楼层
□ NPC 可以自主完成工作
```

## Save

```text
□ Terrain 恢复
□ Building 恢复
□ Object 恢复
□ NPC 恢复
□ Mesh 可以重新生成
```

---

# 74. 当前已验证的硬证据

根据 2026-09-11 Roadmap / self-check：

```text
Phase 0      ✅
Phase 1      ✅
V2-15        ✅
V2-16        ✅
V2-18        ✅ basic
V2-19        ✅ basic
V2-20        ✅
V2-23        ✅ simplified
V2-24        ✅
```

已经验证的具体行为：

### Floor

```text
floor detection
```

### Room

```text
8 × 6 room = 48m²
```

### Portal

```text
door_south
stair_main
```

### Cross-floor route

```text
outdoors
 → door
 → room_0_0
 → stair
 → room_1_0
```

### Furniture obstacle

添加桌子后 Local Navigation 可以绕行。

### Live wall rebuild

一面分隔墙可以把：

```text
1 room
```

变成：

```text
2 rooms
```

同时 Portal 自动重新绑定正确 Room。

### Openings

Door / Window 不再由外部手工补拓扑边，而是由 Wall 自己产生几何开口。

### NPC

NPC 已经能够：

```text
寻找 WorkPoint
 → 走到楼梯
 → 上楼
 → 到达 Floor 1
 → 到达工作点
 → 工作
```

---

# 75. 已经暴露过的 Bug 与必须保留的修复规则

## 74.1 Stuck Detection

不能把“走得久”直接当作“被卡住”。

只在：

```text
Agent not moving
```

时累积 stuck time。

---

## 74.2 Ramp Collider

坡道必须从低端进入，且顶部必须有 Landing。

---

## 74.3 Floor Collision

楼板是真正的 Collider，不能只是渲染表面。

---

## 74.4 Arrival Check

走完 waypoint != 到达。

最终：

```text
distance(actual_position, target)
```

必须满足阈值。

---

## 74.5 Stairwell 两层处理

楼梯口可能在：

- 下层为实体斜坡。
- 上层为楼梯孔。

两个楼层都必须进行正确的障碍判定。

---

## 74.6 InteractionPoint World Transform

InteractionPoint 在 Planning 时必须已经是正确 World Position，不能等待 `_process` 才更新。

---

## 74.7 T-Junction

必须进行 Planar Subdivision。

---

# 76. Roadmap V2.1：重新排序后的真实开发路线

## Phase 0 — Technical Spike

```text
✅ 已完成
```

---

## Phase 1 — Spatial Core

```text
✅ 已完成
```

包含：

```text
Floor
Room
Portal
Room Graph
Local Navigation
```

---

## Phase 2 — Terrain Foundation

下一核心阶段：

```text
V2-10  Fine Grid + Chunk
V2-11  Materials + Dig / Fill
V2-12  Polygon + Road Rasterization
V2-13  Buildability + Foundation
```

V2.1 新增正式子目标：

```text
V2-10a TerrainCell
V2-10b TerrainChunk
V2-11a MaterialDefinition
V2-11b TerrainModifier
V2-12a Polygon Rasterizer
V2-13a BuildableMask
V2-13b FoundationValidator
```

---

## Phase 3 — Procedural Building

```text
V2-14 Building Intent
V2-15 Wall Connection Solver      ✅
V2-16 Openings                    ✅
V2-17 Roof Generator
```

但是正式 Building Placement 必须经过：

```text
Terrain Buildability
Foundation Validation
Material Cost
```

现有 `V2-14 drag-to-place wall` 原型继续保留为技术测试工具，不视为最终完整建筑流程。

---

## Phase 4 — Object / Interaction

```text
V2-18 WorldObject                 ✅ basic
V2-19 Free Placement              ✅ basic
V2-20 InteractionPoint            ✅
V2-21 Containers
```

下一步重点：

```text
Container
Inventory
Material Access
Object Definition
Placement Validation
```

---

## Phase 5 — NPC

```text
V2-22 NPC Data Model
V2-23 Job / Task                   ✅ simplified
V2-24 Cross-floor Work             ✅
V2-25 Schedule / Needs
```

下一步重点：

```text
Data-driven NPC State
Needs
Schedule
Production Task
```

---

## Phase 6 — Persistence

```text
V2-26 Save / Load
```

重点：

```text
World Facts
Definition IDs
Seeds
Terrain Chunks
Building State
Object State
NPC State
```

---

# 77. Phase 7 — Gameplay

Vertical Slice 完成后才正式加入：

```text
农业
生产链
资源
探索
战斗
任务
升级
经济
```

---

# 78. Phase 8 — World

再加入：

```text
世界树
村庄
魔域
飞艇
热气球
坐骑
地下区域
大地图
特殊生态
```

这些系统必须建立在前面稳定的 World Core 上，而不是提前污染核心架构。

---

# 79. 魔域如何复用程序化建造系统

魔域不应该创建第二套 BuildingSystem。

而应该：

```text
Same Building State
Same Wall Graph
Same Room Graph
Same Roof Generator
```

只是：

```text
Style = Corrupted
Material = MagicStone
Decoration = Organic / Crystal / Vein
Terrain = CorruptedGround
```

即：

> **换规则与美术参数，不换底层建造系统。**

---

# 80. 世界树如何接入

世界树不是普通装饰，而可以成为：

```text
World Landmark
Resource Hub
Narrative Hub
Navigation Landmark
Community Center
```

但在 Vertical Slice 前，不进入核心开发依赖。

---

# 81. Airship / Mount 如何接入

飞艇、热气球、坐骑未来统一看作：

```text
WorldObject / Transport Entity
```

具有：

```text
InteractionPoint
StoragePoint
PassengerPoint
RepairPoint
DockPoint
```

而不是提前修改整个 NPC Core。

---

# 82. Hard Non-goals：Vertical Slice 前禁止做

明确禁止：

```text
❌ 大型开放世界
❌ 多人联机
❌ 复杂战斗
❌ 100+ NPC
❌ 完整经济系统
❌ 世界树完整剧情
❌ 魔域完整系统
❌ 飞艇完整驾驶
❌ 大规模天气
❌ 全局流体
❌ 无限程序化世界
❌ 复杂遗传 / 成长系统
```

因为真正危险的不是“代码写不出来”，而是：

> **系统依赖蔓延。**

---

# 83. Claude Code 开发规则

每一个开发任务都必须满足：

```text
One Block
↓
One Testable Goal
↓
Run Self-check
↓
Demo Still Runnable
```

不能一次要求：

```text
“把 Terrain + Building + NPC + Save 全部完成。”
```

应该拆成：

```text
V2-10
TerrainChunk
```

然后：

```text
V2-11
MaterialField
```

再：

```text
V2-12
Polygon Rasterization
```

---

# 84. 每个 Block 的 Definition of Done

每一块至少回答：

```text
1. 输入是什么？
2. State 是什么？
3. Generator / Solver 是什么？
4. Runtime Node 是什么？
5. 如何测试？
6. 如何证明没有破坏之前系统？
```

以及：

```text
Existing Self-check
必须全部继续通过。
```

---

# 85. 第一批实际 Coding Blocks

建议 Claude Code 按以下顺序：

### V2-10a TerrainCell

目标：

```text
定义 TerrainCell 数据
```

验收：

```text
能序列化 / 反序列化
```

### V2-10b TerrainChunk

目标：

```text
Chunk 存储 Cell
```

验收：

```text
创建 / 修改 / dirty / rebuild
```

### V2-11a MaterialDefinition

目标：

```text
Grass / Soil / Sand / Stone / Water
```

验收：

```text
Data-driven
```

### V2-11b TerrainModifier

目标：

```text
Brush + Paint + Dig + Fill
```

验收：

```text
局部修改
```

### V2-12 Polygon Rasterizer

目标：

```text
Triangle → Cells
```

验收：

```text
三角形草地 → Soil
```

### V2-13 BuildableMask

目标：

```text
Terrain → CanBuild
```

验收：

```text
Grass 不可建
Cleared Soil 可建
```

### V2-13b Foundation

目标：

```text
Building Footprint → Support Check
```

验收：

```text
平地可建
过大坡度拒绝
```

---

# 86. 第一个完整 Terrain → Building Demo

最终第一次真正有“游戏感”的 Demo 应该是：

```text
玩家
 ↓
走到一片草地
 ↓
切换 Terrain Tool
 ↓
画一个三角形
 ↓
草地变成土地
 ↓
切换 Building Tool
 ↓
系统允许建造
 ↓
拉出第一面墙
 ↓
拉第二面墙
 ↓
自动连接
 ↓
闭合
 ↓
自动 Room
 ↓
系统生成 Floor
 ↓
系统生成 Roof Preview
 ↓
扣除材料
 ↓
建筑真正落地
```

这个 Demo 是 V2.1 当前最重要的“建造系统里程碑”。

---

# 87. 最终架构哲学

整个项目最终应该遵循四句话：

> **Spatial 决定世界在哪里。**

> **Data 决定世界是什么。**

> **Rules / Systems 决定世界如何运行。**

> **Renderer 决定世界看起来怎样。**

进一步压缩成：

```text
Spatial
  = Where

Data
  = What

Rules / Systems
  = How

Renderer
  = Appearance
```

---

# 88. 最终世界循环

```text
玩家
 ↓
改变土地
 ↓
Terrain
 ↓
获得可建空间
 ↓
Building
 ↓
创建房间
 ↓
WorldObject
 ↓
InteractionPoint
 ↓
NPC
 ↓
Job / Task
 ↓
Navigation
 ↓
Production / Life
 ↓
World State
 ↓
改变环境
 ↓
玩家再次规划
```

这就是项目真正区别于普通建造游戏的地方。

---

# 89. 最终目标画面背后的技术

最终玩家看到的可能只是：

```text
☁️        ☁️

      🌳

   ┌─────────────┐
   │   纺织机    │
   │             │
   └──────┬──────┘
          │
         楼梯
          │
   ┌──────┴──────┐
   │   研究桌    │
   │             │
~~~土地~~~玩家~~~
```

但底层已经包含：

```text
3D Spatial
Floor
Room
Portal
Terrain Field
Buildable Mask
Foundation
Wall Graph
Room Detection
Roof Generator
WorldObject
Collision
Interaction
Navigation
Job
Task
Inventory
Production
Save / Load
Occlusion
Pixel Renderer
```

如果这一小块真的跑通：

> 世界树、村庄、魔域、飞艇、坐骑、远方大陆都只是建立在这套 Core 之上的内容层，而不是重新写一套游戏。

---

# 90. 最终工程顺序

锁定为：

```text
SPACE
 ↓
TERRAIN
 ↓
BUILDING
 ↓
OBJECT
 ↓
INTERACTION
 ↓
NPC
 ↓
PRODUCTION
 ↓
GAMEPLAY
 ↓
WORLD
 ↓
POLISH
```

其中 V2.1 对 Terrain / Building 的内部顺序进一步细化：

```text
Terrain Cell
 ↓
Terrain Chunk
 ↓
Material Field
 ↓
Terrain Modification
 ↓
Buildable Mask
 ↓
Foundation
 ↓
Building Intent
 ↓
Wall Graph
 ↓
Room Graph
 ↓
Roof / Openings
 ↓
WorldObject
 ↓
Interaction
 ↓
NPC
```

---

# 91. 最终一句话

> **让玩家表达意图，让 Terrain 决定土地，让 Building Solver 决定结构，让 WorldObject 填充生活，让 NPC 使用世界，让数据保存事实，让 Renderer 把这一切呈现成一幅会呼吸的 Pixel Art 世界。**

---

# Appendix A：当前 Block Roadmap（2026-09-11）

| Block | 内容 | 状态 |
|---|---|---|
| V2-00 | Skeleton + Self-check | ✅ |
| V2-01 | Orthographic Camera | ✅ |
| V2-02 | Segment Wall | ✅ |
| V2-03 | Character Billboard | ✅ |
| V2-04 | Occlusion Fade | ✅ |
| V2-05 | Floor System | ✅ |
| V2-06 | Room Detection | ✅ |
| V2-07 | Portal | ✅ |
| V2-08 | Room Graph | ✅ |
| V2-09 | Local Navigation | ✅ |
| V2-10 | Fine Grid + Chunk | ⬜ |
| V2-11 | Materials + Dig / Fill | ⬜ |
| V2-12 | Road Rasterization | ⬜ |
| V2-13 | Foundation | ⬜ |
| V2-14 | Building Intent | 🚧 原型可用 |
| V2-15 | Wall Connection Solver | ✅ |
| V2-16 | Openings | ✅ |
| V2-17 | Roof Generator | ⬜ |
| V2-18 | WorldObject | ✅ basic |
| V2-19 | Free Placement | ✅ basic |
| V2-20 | InteractionPoint | ✅ |
| V2-21 | Containers | ⬜ |
| V2-22 | NPC Data Model | ⬜ |
| V2-23 | Job / Task | ✅ simplified |
| V2-24 | Cross-floor Work | ✅ |
| V2-25 | Schedule / Needs | ⬜ |
| V2-26 | Save / Load | ⬜ |
| ART-00 | Art Bible + Master Style Sheet | ⬜ — 设计已锁定，资源未制作 |
| ART-01 | Base Asset Library | ⬜ — 先占位 |
| ART-02 | Terrain Material Library | ⬜ — 先占位 |
| ART-03 | Vegetation Scatter System | ⬜ |
| ART-04 | Procedural Detail Layer | ⬜ |
| ART-05 | AI Asset Ingest / Cleanup Pipeline | ⬜ |
| ART-06 | VFX Definition + VFX Grammar | ⬜ |
| ART-07 | Pixel Treatment / Palette / Lighting | ⬜ — renderer 已有基础路线 |
| ART-08 | Asset Validation | ⬜ |

---

# Appendix B：V2.1 新增设计项

本整合版新增或正式化：

```text
1. Terrain Preparation
2. Buildable Mask
3. Foundation Validator
4. Terrain Modification Intent
5. Polygon Terrain Rasterization
6. Geometry-derived Building Cost
7. Construction Materials → Inventory
8. Art Grammar
9. Base Asset / Procedural Assembly / Procedural Detail
10. Deterministic Art Seed
11. Local Noita-like Material Simulation Boundary
12. Building Dirty Dependency Graph
13. Terrain Dirty Region
14. Terrain → Building → Material → NPC 全链路
15. 当前仓库已验证状态与未来设计状态的明确区分
16. Art Pipeline：Art Bible → AI / Procedural → Asset Library → Runtime Generator
```

---

# Appendix C：最重要的工程禁忌

```text
❌ 不要把 Mesh 当成 State
❌ 不要让 UI 直接改 Mesh
❌ 不要让 NPC 直接寻找具体模型
❌ 不要为每种家具写特殊 NPC 逻辑
❌ 不要把建筑逻辑写死在 Prefab
❌ 不要让 Terrain 变成只能换 Tile 的系统
❌ 不要把 Noita 式全局模拟提前做进核心
❌ 不要在 Vertical Slice 前做完整开放世界
❌ 不要把所有随机美术做成不可复现的 Random
❌ 不要为了一个新物品修改多个核心系统
❌ 不要让一个局部改动触发全世界重建
```

---

# Appendix D：工程最高优先级

任何新功能都先问：

```text
它属于哪一个 State？
它如何被 Definition 描述？
它如何通过 Rule / Solver 改变世界？
它如何生成 Runtime Representation？
它是否需要 Dirty Region？
它是否影响 Navigation？
它是否需要 InteractionPoint？
它需要保存哪些 World Facts？
```

如果这些问题答不出来，不应该直接开始写 UI 或 Mesh。


---

# Appendix E：V2 美术资产与渲染管线（Fixed Camera Pixel World）

> 本章正式锁定 V2 的美术生产方式。
>
> **固定相机角度 + 真实 3D 建筑 + 像素化非建筑资产 + 像素 VFX + 统一色板/灯光。**
>
> 本章中的“无冕之乡式”仅表示目标视觉方向的参考，不声称复刻其内部源码或完整技术管线。公开开发资料可以支持其使用 Godot、WorldEnvironment、灯光/阴影与像素化相关 Shader 的方向，但无法据此断言其全部资产生产方式。

## E.1 美术总原则

### E.1.1 固定机位

相机采用固定或严格受控的 Orthographic Camera：

```text
Camera
├── Position：锁定
├── Rotation：锁定
├── Orthographic：true
├── Zoom：有限档位
└── Pixel Framing：锁定
```

第一版禁止让玩家自由旋转相机作为核心玩法要求。

原因：固定机位可以让我们为一个确定的观察方向制作像素资产，从而显著降低植物、NPC、VFX 和环境装饰的生产成本，同时提高 AI 生成资产的一致性。

### E.1.2 “3D”与“Pixel Art”不是二选一

```text
真实 3D
    ↓
负责：
位置 / 楼层 / 碰撞 / 导航 / 建筑结构 / Terrain 高度

Pixel Art
    ↓
负责：
人物 / 植物 / 小物件 / VFX / 材质细节 / 色彩语言
```

因此：

> **3D 是空间标准，不是所有美术资产必须使用 3D 模型。**

### E.1.3 建筑是主要的 3D 资产类别

建筑必须继续使用真实 3D Geometry，因为它需要：

```text
Wall
Floor
Roof
Door
Window
Stair
Foundation
Column
Beam
Collision
Room
Navigation
```

建筑视觉：

```text
3D Geometry
    +
Pixel-oriented Material
    +
Stylized Lighting
    +
Procedural Detail
```

### E.1.4 建筑之外默认优先 Pixel Asset

除少量必须 3D 化的特殊世界对象外，以下默认采用 Pixel Sprite / Billboard / Card：

```text
Grass
Flowers
Fern
Bush
Leaves
Small Tree Canopy
Small Rocks
Pebbles
Mushrooms
NPC
Animals
Fire
Smoke
Dust
Magic Particles
Leaves
Rain
Snow
Small Decorations
```

---

# E.2 最终渲染架构

```text
                         WORLD
                           │
          ┌────────────────┼─────────────────┐
          │                │                 │
      BUILDING          TERRAIN          PIXEL ASSETS
          │                │                 │
    Real 3D Geometry   3D Surface      Sprite/Billboard
          │                │                 │
    Pixel Material     Pixel Ground       Grass / Tree
          │                │              NPC / VFX
          └────────────────┼─────────────────┘
                           ↓
                    Fixed Orthographic
                           ↓
                  Stylized 3D Lighting
                           ↓
                     Pixel-oriented
                       Shadow/VFX
                           ↓
                     Color Grading
                           ↓
                    Optional Pixelation
                           ↓
                         Screen
```

**注意：** 不强制所有画面最终缩到同一个极低分辨率再放大。第一版优先让资产本身具备正确像素密度，再根据实际截图决定是否增加全屏低分辨率后处理。

---

# E.3 Asset Library 总目录

推荐项目目录：

```text
assets/
└── art/
    ├── bible/
    │   ├── master_style.md
    │   ├── palette_master.png
    │   ├── lighting_reference.png
    │   ├── camera_reference.png
    │   └── scale_reference.png
    │
    ├── references/
    │   ├── environment/
    │   ├── character/
    │   ├── architecture/
    │   └── vfx/
    │
    ├── pixel/
    │   ├── terrain/
    │   ├── vegetation/
    │   ├── environment/
    │   ├── props/
    │   ├── characters/
    │   ├── animals/
    │   ├── vfx/
    │   └── ui/
    │
    ├── building/
    │   ├── materials/
    │   ├── modules/
    │   ├── roofs/
    │   ├── openings/
    │   └── details/
    │
    ├── atlases/
    │   ├── vegetation/
    │   ├── characters/
    │   ├── vfx/
    │   └── props/
    │
    ├── palettes/
    │   ├── world_day.png
    │   ├── world_night.png
    │   ├── magic.png
    │   ├── corrupted.png
    │   └── ui.png
    │
    └── generated/
        ├── ai_raw/
        ├── cleaned/
        ├── approved/
        └── rejected/
```

### E.3.1 三种资产状态

每个素材必须拥有明确状态：

```text
RAW
 ↓
CLEANED
 ↓
APPROVED
```

`RAW`：AI 或人工第一次产生的原始素材。

`CLEANED`：去背景、修像素、统一调色、修比例之后的素材。

`APPROVED`：已经通过游戏内实际截图验证，可以进入正式 Asset Library。

禁止直接把 AI 原图放进正式运行目录。

---

# E.4 Asset Definition

每一个正式资产必须拥有 Definition 元数据。

示例：

```json
{
  "id": "grass_tuft_01",
  "category": "vegetation",
  "subcategory": "grass",
  "style_set": "cozy_pixel_world",
  "source": "ai_cleaned",
  "license": "project_verified",
  "resolution": [32, 32],
  "pixel_scale": 1,
  "pivot": [0.5, 1.0],
  "billboard": true,
  "casts_shadow": false,
  "biomes": ["grassland", "village", "forest_edge"],
  "variants": 6,
  "tags": ["small", "ground", "vegetation"]
}
```

### E.4.1 必须保存的字段

```text
id
category
subcategory
style_set
source
license
resolution
pixel_scale
pivot
anchor
billboard
casts_shadow
biomes
tags
variants
```

### E.4.2 为什么必须有 Asset Definition

因为以后可能有：

```text
100 种草
50 种花
30 种石头
40 个 VFX
20 个 NPC
```

如果只靠文件名，不足以让 Runtime Generator 判断：

```text
这个东西能不能放在水边？
能不能生成在道路旁？
是不是森林植物？
是不是需要阴影？
是不是只能出现在魔域？
```

---

# E.5 命名规范

统一：

```text
<category>_<type>_<style>_<variant>_<direction>_<action>_<frame>
```

例如：

```text
grass_tuft_cozy_01.png
flower_daisy_cozy_03.png
rock_small_stone_02.png
tree_oak_cozy_01_back.png
npc_villager_01_front_idle_00.png
npc_villager_01_front_walk_03.png
npc_villager_01_side_work_06.png
vfx_fire_small_01_07.png
vfx_magic_worldtree_02_12.png
```

### E.5.1 不允许

```text
final.png
final2.png
new.png
new_new.png
草.png
好看的树.png
```

---

# E.6 像素规格标准

由于本项目固定机位，像素密度必须锁定。

建议第一版建立三个规格：

```text
Small   = 16~32 px 高
Medium  = 32~64 px 高
Large   = 64~128 px 高
```

例如：

```text
小草       16~24px
花         16~24px
小石头     16~24px
灌木       32~48px
NPC        48~96px
小动物     32~64px
树         64~128px
大型特殊树  128px+
```

实际数值由游戏最终 Camera Zoom 和截图测试锁定；以上是资产生产初始规范，不是硬编码。

### E.6.1 统一像素密度

最重要的不是“全部 32×32”，而是：

> **在最终固定 Camera 下，同一世界里的物体具有相近的视觉像素密度。**

---

# E.7 Pixel Asset 生产标准

AI 或人工生成的像素资产必须尽量满足：

```text
Transparent Background
Fixed View
Clean Silhouette
Limited Palette
Readable at Small Size
No Photorealism
No Smooth Vector Gradients
No Anti-Aliasing
No Text
No Watermark
No UI
No Perspective Drift
```

### E.7.1 推荐 Negative Prompt

```text
photorealistic, realistic photo, 3d render, smooth vector art,
soft airbrush, excessive gradients, blurry edges, anti-aliased edges,
text, letters, logo, watermark, background scenery, multiple objects,
complex perspective, fisheye, dramatic camera angle, excessive detail,
noise, over-rendered texture
```

---

# E.8 Master Style Prompt

所有 AI 资产优先在同一个 Style Block 上叠加。

```text
cozy stylized pixel art for a 3D orthographic game world,
hand-crafted pixel clusters,
fixed isometric-like camera view,
clear readable silhouette,
muted warm natural palette,
soft warm highlights,
cool soft shadows,
controlled pixel density,
limited color palette,
subtle asymmetry,
hand-painted feeling,
low visual noise,
small-scale readable details,
medieval fantasy countryside atmosphere,
soft charming diorama feeling,
transparent background,
game asset, production-ready sprite
```

这段不是每次必须一字不改；它是整个项目的 **Master Style Block**。

---

# E.9 Terrain Asset Library

## E.9.1 Grass Ground

### 需要的基础资产

```text
grass_base
grass_dark_patch
grass_light_patch
grass_dry_patch
grass_edge
grass_tuft_small
grass_tuft_large
```

### Prompt

```text
stylized hand-crafted pixel art grass ground texture,
cozy medieval fantasy countryside,
soft natural green palette,
subtle warm yellow-green highlights,
cool dark green shadow clusters,
irregular organic color patches,
not tile-like, not checkerboard,
small readable pixel clusters,
low visual noise,
seamless game texture,
orthographic/isometric compatible
```

## E.9.2 Soil

```text
hand-crafted pixel art soil texture,
rich warm brown soil,
small irregular darker clumps,
subtle stones and organic variation,
cozy fantasy farming game,
limited earthy palette,
readable at small scale,
seamless game texture,
no photorealism
```

## E.9.3 Sand

```text
stylized pixel art sandy ground texture,
soft beige and warm ochre palette,
small granular pixel clusters,
irregular natural variation,
cozy fantasy game environment,
seamless texture,
no photorealistic sand
```

## E.9.4 Stone Ground

```text
stylized medieval pixel art stone ground,
irregular flat stones,
muted gray-brown palette,
soft cool shadows between stones,
slight moss and dirt accumulation,
hand-crafted pixel clusters,
seamless game texture
```

## E.9.5 Mud

```text
stylized pixel art muddy ground,
warm dark brown and cool wet brown palette,
soft irregular puddle patches,
small footprints and disturbed soil shapes,
cozy fantasy game,
readable at small scale,
no realistic mud photography
```

---

# E.10 Road Asset Library

道路不依赖大量固定 Road Tile，而优先使用：

```text
Path Spline
  ↓
Road Mask
  ↓
Pixel Ground Material
  ↓
Edge Scatter
```

### Road Prompt

```text
hand-crafted pixel art medieval countryside dirt path,
soft irregular edges,
warm brown and muted green palette,
grass naturally encroaching on the sides,
small pebbles and wheel marks,
cozy fantasy village atmosphere,
readable pixel clusters,
not a perfectly straight tile texture
```

### Stone Road Prompt

```text
stylized pixel art medieval cobblestone path,
irregular stones,
soft warm gray and brown palette,
grass growing between cracks,
hand-placed pixel clusters,
cozy fantasy village,
seamless game texture,
no photorealism
```

---

# E.11 Vegetation Library

## E.11.1 Grass Tuft

```text
hand-crafted pixel art grass tuft,
small readable silhouette,
soft natural green palette,
warm yellow-green highlights,
cool dark green shadows,
slightly asymmetrical blades,
transparent background,
fixed orthographic game view,
cozy fantasy countryside,
limited color palette,
no outline-heavy cartoon style
```

## E.11.2 Flower

```text
single stylized pixel art wildflower,
small medieval countryside flower,
clear readable silhouette,
soft pastel petals,
small green stem and leaves,
limited palette,
transparent background,
fixed orthographic game view,
cozy fantasy game asset
```

## E.11.3 Fern

```text
stylized pixel art fern,
small dense forest floor plant,
curved layered leaves,
deep green shadow clusters,
muted natural palette,
transparent background,
hand-crafted pixel clusters,
fixed orthographic view
```

## E.11.4 Bush

```text
stylized pixel art countryside bush,
rounded organic silhouette,
soft layered foliage,
muted green palette,
small shadow clusters,
slightly irregular shape,
transparent background,
fixed game camera angle
```

## E.11.5 Mushroom

```text
tiny stylized pixel art fantasy mushroom,
small rounded cap,
muted warm red or cream palette,
short stem,
readable at small scale,
transparent background,
cozy woodland game asset
```

---

# E.12 Tree Library

普通树默认：

```text
3D Trunk / simplified trunk option
+
Pixel Canopy Card
```

特殊大树才使用真正复杂 3D。

## E.12.1 Oak Canopy

```text
hand-crafted pixel art tree canopy,
large rounded medieval oak crown,
layered clusters of leaves,
muted natural greens,
warm highlights,
cool deep shadows,
irregular organic silhouette,
transparent background,
fixed orthographic game view,
cozy fantasy countryside,
no photorealism
```

## E.12.2 Autumn Tree

```text
stylized hand-crafted pixel art autumn tree canopy,
muted amber orange red and olive green leaves,
soft clustered foliage,
irregular silhouette,
cozy medieval fantasy countryside,
transparent background,
fixed orthographic game view
```

## E.12.3 Dead Tree

```text
stylized pixel art dead tree canopy and branches,
muted gray brown palette,
small sparse leaves,
slightly eerie but not horror,
fantasy countryside,
transparent background,
fixed orthographic game view
```

---

# E.13 Small Environment Props

资产库至少规划：

```text
pebble_small
pebble_cluster
stick
fallen_branch
fallen_leaf
leaf_pile
dirt_clump
mud_clump
grass_patch
flower_patch
moss_patch
mushroom_cluster
small_puddle
water_ripple
```

### Pebble Prompt

```text
small hand-crafted pixel art pebble cluster,
three to five irregular natural stones,
muted gray brown palette,
soft tiny shadows,
transparent background,
cozy fantasy game prop,
small readable silhouette
```

### Leaf Pile Prompt

```text
small stylized pixel art pile of fallen leaves,
muted warm green yellow brown colors,
hand-placed pixel clusters,
transparent background,
cozy woodland game environment,
readable at small scale
```

---

# E.14 Water / Shore Assets

水体逻辑依然由 TerrainSystem 决定，视觉由 Pixel Assets 提供：

```text
water_surface
water_edge
shore_grass
shore_stone
shore_mud
ripple_small
ripple_large
foam_small
```

### Water Prompt

```text
stylized pixel art calm fantasy pond surface,
soft muted blue-green water,
small horizontal pixel highlights,
subtle natural ripple patterns,
cozy medieval countryside,
limited palette,
no photorealistic reflections,
seamless game texture
```

### Shore Prompt

```text
stylized pixel art pond shoreline,
soft transition from grass to mud to water,
irregular natural edge,
small stones and grass clumps,
muted green brown and blue-green palette,
cozy fantasy game environment
```

---

# E.15 Rock / Cliff Library

## Small Rock

```text
small stylized pixel art fantasy rock,
irregular natural silhouette,
muted gray brown stone palette,
soft warm highlight,
cool shadow side,
subtle moss patch,
transparent background,
fixed orthographic game view
```

## Large Rock

```text
large stylized medieval fantasy boulder,
clear readable silhouette,
low visual detail,
muted gray green brown palette,
small moss clusters,
soft stylized shading,
transparent background,
fixed orthographic game view
```

## Cliff Texture

建筑/地形真正的悬崖边缘优先采用 3D Geometry + Pixel Texture：

```text
stylized pixel art layered stone cliff texture,
stratified irregular rocks,
muted gray-brown and moss green palette,
soft stylized depth clusters,
seamless texture,
medieval fantasy world
```

---

# E.16 Architecture Texture Library

建筑结构本身由 BuildingGenerator 生成；以下只是 Material Library。

```text
stone_wall
rough_stone
brick
old_brick
wood_plank
wood_beam
wood_log
painted_wood
roof_tile
roof_shingle
plaster
aged_plaster
metal_dark
iron
copper
```

## E.16.1 Stone Wall Prompt

```text
hand-painted stylized pixel art medieval stone wall material,
irregular stone blocks,
muted gray warm brown palette,
small cool shadow clusters,
subtle moss and weathering,
clear readable pixel structure,
seamless game texture,
not photorealistic
```

## E.16.2 Wood Plank Prompt

```text
stylized pixel art medieval wood plank material,
hand-painted wood grain,
muted warm brown palette,
irregular plank width,
subtle dark seams,
small weathered marks,
readable at small scale,
seamless game texture
```

## E.16.3 Roof Prompt

```text
stylized pixel art medieval roof tile material,
muted terracotta brown or dark slate palette,
repeating but slightly irregular tile shapes,
soft warm highlights,
cool shadow clusters,
cozy fantasy village,
seamless game texture
```

---

# E.17 Furniture / Props Library

建议首批：

```text
chair
bench
table
bed
shelf
cabinet
chest
crate
barrel
basket
lantern
candle
fireplace
workbench
forge
research_table
textile_machine
cooking_pot
alchemy_table
magic_table
```

对于家具，不要求 AI 直接输出最终 3D 模型。优先：

```text
AI Concept / Pixel Reference
        ↓
固定风格设计
        ↓
需要 3D 的物体 → Blender / Meshy → 3D
不需要 3D 的物体 → Pixel Sprite
```

Meshy 当前支持文本/图片到 3D、Remesh、AI 纹理和角色自动绑定/动画，适合快速做原型和占位资产；最终正式资产仍应经过人工拓扑、尺度、材质与版权审查。citeturn653595search0turn653595search2

---

# E.18 VFX Library

VFX 一律优先 Pixel Sprite / Sprite Sheet + 粒子系统。

首批：

```text
fire_small
fire_campfire
smoke_small
smoke_chimney
steam
spark
spark_large
dust_step
dust_build
leaf_fall
leaf_swirl
rain
snow
magic_mote
magic_burst
magic_circle
worldtree_particle
corruption_particle
water_splash
water_ripple
```

## E.18.1 Fire Prompt

```text
hand-crafted pixel art small campfire flame,
three to five animation frames,
clear readable flame silhouette,
warm orange yellow red palette,
dark red edge pixels,
transparent background,
cozy fantasy game VFX sprite sheet,
consistent frame size,
no smooth gradients
```

## E.18.2 Smoke Prompt

```text
stylized pixel art soft chimney smoke,
small rounded pixel clusters,
muted gray warm brown palette,
transparent background,
three to five animation frames,
cozy medieval fantasy game VFX,
no realistic volumetric smoke
```

## E.18.3 Magic Prompt

```text
hand-crafted pixel art fantasy magic particles,
small glowing cyan teal pale violet motes,
limited palette,
clear pixel clusters,
transparent background,
sprite sheet,
cozy magical fantasy game,
not photorealistic,
no text
```

## E.18.4 World Tree Prompt

```text
stylized pixel art sacred world tree magical particles,
small floating luminous motes,
soft cyan pale green and warm gold palette,
hand-crafted pixel clusters,
transparent background,
fantasy game VFX sprite sheet,
cozy mysterious atmosphere,
readable at small scale
```

---

# E.19 Biome Asset Grammars

不要让 Terrain 直接“随机生成任何植物”。

Terrain Material + Biome Grammar 决定可生成资产。

## E.19.1 Grassland

```text
Grass
├── GrassTuft 1.00
├── Flower 0.20
├── Pebble 0.08
├── SmallBush 0.05
├── FallenLeaf 0.03
└── DirtPatch 0.10
```

## E.19.2 Forest Edge

```text
Grass
├── GrassTuft 0.70
├── Fern 0.40
├── Flower 0.15
├── Bush 0.25
├── Pebble 0.15
├── Moss 0.20
└── FallenBranch 0.08
```

## E.19.3 Village

```text
Grass
├── GrassTuft 0.25
├── Flower 0.08
├── Pebble 0.12
├── DirtPatch 0.25
├── FurnitureOutdoor 0.05
└── RoadEdgeDetail 0.30
```

## E.19.4 Magic / Corrupted Terrain

```text
CorruptedGround
├── DeadGrass
├── Crystal
├── DarkMoss
├── MagicMote
├── CorruptionCrack
└── FloatingDust
```

---

# E.20 Procedural Scatter 规则

Scatter 不是纯随机，而是：

```text
Terrain Material
+
Biome
+
Slope
+
Moisture
+
Fertility
+
Distance to Road
+
Distance to Building
+
Seed
```

### E.20.1 基本公式概念

```text
SpawnProbability
=
BaseDensity
× BiomeMultiplier
× TerrainMultiplier
× SlopeMultiplier
× DistanceMultiplier
× Random(seed)
```

### E.20.2 建筑边缘

建筑周围不应出现：

```text
厚草
大石头
大树
```

可以增加：

```text
小草
泥
石屑
木屑
脚印
```

### E.20.3 道路边缘

道路本体：

```text
Grass Density ↓
```

道路边缘：

```text
Grass Density ↑
Pebble ↑
Dirt ↑
```

这样可以自然产生边缘。

---

# E.21 Deterministic Art Seed

所有程序化视觉变化必须可复现：

```text
WorldSeed
   +
ChunkCoord
   +
ObjectID
   +
ArtRuleID
   ↓
Deterministic Seed
```

相同：

```text
世界
位置
资产 Definition
规则
```

必须生成相同结果。

这样存档加载不会出现：

```text
昨天这棵树在左边
今天跑到右边
```

---

# E.22 AI Asset Production Pipeline

标准管线：

```text
Reference / Prompt
       ↓
AI Generation
       ↓
Select Best Variant
       ↓
Background Removal
       ↓
Crop / Anchor
       ↓
Pixel Cleanup
       ↓
Palette Correction
       ↓
Scale Validation
       ↓
Game Screenshot Test
       ↓
APPROVED
       ↓
Asset Library
```

### E.22.1 AI 不允许直接成为 Runtime Generator

AI 用于：

```text
概念
原子资产
变体
贴图参考
VFX 参考
角色设计
```

Runtime 使用：

```text
预先生成并审核的资产
+
程序化组合
```

这样性能、可复现性和风格才能控制。

---

# E.23 推荐工具链

## E.23.1 Pixelorama

适合：

```text
像素资产清理
逐帧动画
Palette
Sprite Sheet
```

Pixelorama 是开源像素画工具，提供逐帧动画、Onion Skin、Frame Tags、Sprite Sheet 导出等能力。citeturn699087search1turn699087search6

## E.23.2 Aseprite

适合：

```text
专业 Pixel Art
逐帧动画
Sprite Sheet
Texture Atlas
批量 CLI 导出
```

Aseprite 官方支持动画帧、Frame Tags、Onion Skin、Sprite Sheet、Texture Atlas 和 CLI 导出。citeturn699087search3turn699087search9

## E.23.3 Scenario

适合：

```text
批量游戏资产生成
风格一致性
Style Reference
Character Reference
Pixelate
```

Scenario 官方文档提供 Reference、Style Reference、Character Reference 和结构控制，并有专门的 Pixelate 工具。citeturn653595search6turn653595search7turn653595search15

## E.23.4 Meshy

主要用于：

```text
建筑外观参考
家具/道具
树干/岩石等真正需要 3D 的对象
快速原型
```

当前 Meshy 支持 Text-to-3D、Image-to-3D、Remesh、AI Texturing、Auto Rigging，并支持导出多种常见格式；官方也明确把它定位为游戏资产原型和生产流程工具。citeturn653595search2turn653595search5

**重要：** 我们的项目大部分非建筑资产不需要走 Meshy；它主要用于确实需要 3D 的资产。

---

# E.24 AI 工具选择原则

```text
像素草/花/小物件
→ Scenario / 图像生成 + Pixelorama/Aseprite 清理

角色概念 / NPC 设计
→ Scenario / 图像生成 + 人工统一 Character Sheet

真正需要 3D 的道具
→ Meshy / Blender

建筑材质
→ 程序化材质 + AI 参考

最终修像素
→ Pixelorama / Aseprite

引擎运行
→ Godot
```

---

# E.25 NPC 美术系统

NPC 是本项目非常重要的特殊资产：

```text
World Position = 3D
Visual Character = Pixel 2D
Animation = 2D Rig + Pixel Corrections
```

因此 NPC 不使用传统 3D Character Controller 作为视觉角色。

---

# E.26 NPC 的基础资产结构

每个 NPC 建议拆成：

```text
NPC
├── shadow
├── body
├── head
├── hair
├── face
├── torso_clothing
├── arm_L
├── arm_R
├── hand_L
├── hand_R
├── leg_L
├── leg_R
├── foot_L
├── foot_R
├── equipment_back
├── equipment_hand
└── accessories
```

但所有这些最终都可以由同一套 Skeleton 驱动。

---

# E.27 为什么不要把整个人物画成一张图再直接骨骼变形

因为像素艺术与连续网格形变存在天然冲突。

如果整个人物：

```text
一个 Sprite
    ↓
Smooth Bone Deformation
```

很容易出现：

```text
像素被拉长
像素被压扁
轮廓出现奇怪锯齿
关节变糊
衣服穿模
```

因此本项目 NPC 默认使用：

> **Rigid Cutout 2D Bone Animation**

而不是大范围 Smooth Mesh Deformation。

---

# E.28 推荐 NPC 骨骼结构

```text
root
└── pelvis
    ├── spine
    │   ├── chest
    │   │   ├── neck
    │   │   │   └── head
    │   │   ├── shoulder_L
    │   │   │   └── upper_arm_L
    │   │   │       └── forearm_L
    │   │   │           └── hand_L
    │   │   └── shoulder_R
    │   │       └── upper_arm_R
    │   │           └── forearm_R
    │   │               └── hand_R
    │   ├── thigh_L
    │   │   └── shin_L
    │   │       └── foot_L
    │   └── thigh_R
    │       └── shin_R
    │           └── foot_R
```

不需要一开始就做：

```text
finger bones
facial bones
toe bones
```

第一版 NPC 不需要这么复杂。

---

# E.29 Godot 2D Skeleton 方案

Godot 原生提供：

```text
Skeleton2D
Bone2D
```

并可以将 2D 多边形绑定到骨骼，使用权重进行变形。官方文档也提供了完整的 Skeleton2D / Bone2D 设置和权重流程。citeturn699087search0turn699087search2

但是我们的像素人物默认不要大量使用权重变形。

建议：

```text
Bone2D
  ↓
Sprite2D 部件
```

例如：

```text
upper_arm_L.png
forearm_L.png
hand_L.png
```

分别挂到：

```text
upper_arm_L Bone
forearm_L Bone
hand_L Bone
```

这样旋转时：

```text
像素图整体旋转
```

不会把图像拉成一团。

---

# E.30 哪些东西适合骨骼，哪些适合逐帧

## 骨骼优先

```text
Idle
Walk
Run
Turn
Carry
Simple Work
Simple Sit
Simple Reach
```

## 逐帧像素修正优先

```text
Attack
Jump
Fall
Heavy Work
Special Crafting
Magic Cast
Complex Emote
Damage
Death
Unique Character Action
```

## 最推荐：Hybrid Animation

```text
Bone Animation
      ↓
基础动作
      ↓
导出 / 播放
      ↓
Pixel Corrective Frames
      ↓
最终动画
```

这样既能减少大量逐帧劳动，又不会让角色失去真正的 Pixel Art 味道。

---

# E.31 NPC 动作库

第一阶段：

```text
idle
walk
walk_start
walk_stop
turn
sit
stand
sleep
wake
carry
pickup
putdown
work
interact
talk
```

第二阶段：

```text
run
climb_stair
push
pull
harvest
plant
cook
craft
repair
read
write
```

第三阶段：

```text
attack
block
hurt
knockback
cast_magic
dodge
die
special_action
```

---

# E.32 NPC 方向设计

由于相机固定，第一版不需要为每一个角度制作完整动画。

推荐：

```text
Front
Back
Side
```

其中 Side 可以先左右镜像；如果角色武器、头发或装备具有强方向性，再拆成：

```text
Left
Right
```

后期如果画面需要更丰富，再扩展：

```text
8 Direction
```

即：

```text
N
NE
E
SE
S
SW
W
NW
```

不要第一天就制作 8 方向 × 20 动作。

---

# E.33 NPC AI 生成 Prompt

## E.33.1 基础 NPC

```text
full-body pixel art RPG villager,
cozy medieval fantasy countryside,
fixed orthographic game camera,
clear readable silhouette,
small stylized proportions,
hand-crafted pixel clusters,
muted warm natural palette,
simple practical medieval clothing,
plain boots,
small readable face,
transparent background,
character game asset,
front-facing,
consistent pixel density,
no background,
no text,
no watermark
```

## E.33.2 NPC Character Sheet

```text
pixel art RPG character sheet,
same character shown in front, back and side views,
cozy medieval fantasy villager,
fixed orthographic game camera,
consistent proportions,
same face, same hair, same clothing,
limited color palette,
clean silhouette,
transparent background,
no text labels,
no background scenery,
game sprite production sheet
```

## E.33.3 工作服

```text
pixel art medieval fantasy village worker,
cozy countryside RPG,
simple linen shirt,
work apron,
leather belt,
practical boots,
slightly worn clothing,
muted warm brown green cream palette,
clear readable silhouette,
transparent background,
fixed orthographic view
```

## E.33.4 魔法研究者

```text
pixel art fantasy researcher NPC,
cozy medieval magic academy aesthetic,
dark teal robe,
cream inner shirt,
small satchel,
subtle glowing accessory,
muted blue green gold palette,
clear readable silhouette,
hand-crafted pixel clusters,
transparent background,
fixed orthographic game view
```

---

# E.34 NPC Layered Clothing System

不要为每一个 NPC 重新生成完整图片。

采用：

```text
Base Body
+
Hair
+
Face
+
Shirt
+
Pants
+
Shoes
+
Accessory
+
Equipment
```

这样：

```text
10 Hair
×
8 Shirts
×
6 Pants
×
5 Accessories
```

理论上即可产生大量组合。

但要注意：

> 组合必须经过视觉验证，不要让随机组合产生不合理穿搭。

---

# E.35 NPC Animation 技术流程

推荐实际工作流：

```text
① 生成 Character Sheet
        ↓
② 清理 Pixel Art
        ↓
③ 拆分 Body Parts
        ↓
④ 建立 Bone2D Skeleton
        ↓
⑤ 绑定 Sprite Parts
        ↓
⑥ 制作 Rest Pose
        ↓
⑦ 制作 Idle
        ↓
⑧ 制作 Walk
        ↓
⑨ 制作 Work / Interact
        ↓
⑩ 添加 Pixel Corrective Frames
        ↓
⑪ 导出 Sprite / Animation
        ↓
⑫ 接入 Godot AnimationPlayer
        ↓
⑬ NPC State 驱动 Animation
```

---

# E.36 NPC Animation 与 Gameplay 解耦

NPC 不直接说：

```text
play_animation("research_table_animation")
```

而是：

```text
NPC State = WORKING
WorkType = RESEARCH
```

Animation Controller 根据：

```text
NPC State
+
Action
+
Direction
+
Equipment
```

选择：

```text
Animation Clip
```

例如：

```text
WORKING + RESEARCH
→ work_research_side

WORKING + TEXTILE
→ work_textile_side

SLEEPING
→ sleep_side
```

---

# E.37 NPC 与 InteractionPoint

NPC 不直接知道家具模型。

它只知道：

```text
InteractionPoint.type = WORK
InteractionPoint.action = RESEARCH
```

执行后：

```text
NPC
 ↓
Walk
 ↓
AlignToInteractionPoint
 ↓
FaceTarget
 ↓
Play Action Animation
 ↓
Wait Duration
 ↓
Apply Result
```

这样同一套 NPC Animation Controller 就可以支持：

```text
研究桌
纺织机
工作台
炼金台
厨房
魔法台
铁匠炉
```

---

# E.38 Pixel Animation 导出标准

Sprite Sheet：

```text
每帧尺寸固定
每行动画固定 Frame Count 或固定时长
透明背景
Nearest Filtering
No Mipmap
```

例如：

```text
NPC Walk Side
6 frames
64 × 64 / frame
```

文件：

```text
npc_villager_01_side_walk.png
npc_villager_01_side_walk.json
```

Aseprite 支持 Sprite Sheet 和 Texture Atlas 导出，也支持 CLI 批量处理，适合后续建立自动化资源流水线。citeturn699087search3turn699087search9

---

# E.39 NPC Shadow

NPC 角色 Sprite 本身不要承担地面阴影。

单独：

```text
shadow_small.png
shadow_medium.png
shadow_large.png
```

根据角色：

```text
NPC
 ↓
Shadow Sprite
 ↓
3D Ground Position
```

这样：

```text
NPC 上楼
NPC 下楼
NPC 坐下
NPC 睡觉
```

阴影都能统一处理。

---

# E.40 固定机位下 NPC 的“假三维”策略

NPC 虽然是 2D Pixel Sprite，但逻辑位置永远是 3D：

```text
NPC.position = Vector3
```

Renderer：

```text
3D Position
    ↓
Camera Projection
    ↓
Sprite Screen Position
```

这样 NPC 可以真正：

```text
站在地面
站在二楼
站在桥上
站在楼梯
站在房间后面
```

但是视觉仍然是一张像素人物。

---

# E.41 NPC 骨骼方案选择

## 方案 A：Godot Skeleton2D

**推荐作为第一版。**

优点：

```text
免费
内置
与 Godot 集成
不用额外运行时
适合我们的项目规模
```

官方文档明确支持 Skeleton2D、Bone2D 和 2D 骨骼变形。citeturn699087search0turn699087search2

## 方案 B：Spine

当 NPC 系统变复杂时再考虑：

```text
大量角色
大量换装
复杂 IK
复杂动画状态
大量可复用动画
```

第一阶段不强制引入 Spine。

## 方案 C：纯逐帧

适合：

```text
主角
重要 NPC
Boss
特殊动作
```

不适合：

```text
100 个普通村民
```

最终推荐：

> **普通 NPC = Bone Rig + 少量 Pixel Corrective Frames；主角/重要 NPC = Hybrid / Frame-by-frame。**

---

# E.42 NPC “绑定骨骼”具体操作规范

### Step 1：先做 Rest Pose

```text
双脚自然站立
双臂略微打开
头正面
躯干直立
```

### Step 2：拆 Sprite

```text
head.png
torso.png
upper_arm_L.png
forearm_L.png
hand_L.png
upper_arm_R.png
forearm_R.png
hand_R.png
thigh_L.png
shin_L.png
foot_L.png
thigh_R.png
shin_R.png
foot_R.png
```

### Step 3：建立 Skeleton2D

```text
root
→ pelvis
→ spine
→ chest
→ neck
→ head
```

### Step 4：建立四肢 Bone2D

```text
chest
├── shoulder_L → upper_arm_L → forearm_L → hand_L
├── shoulder_R → upper_arm_R → forearm_R → hand_R
├── pelvis → thigh_L → shin_L → foot_L
└── pelvis → thigh_R → shin_R → foot_R
```

### Step 5：Sprite 跟随骨骼

每个 Sprite 作为对应 Bone2D 的子节点，优先进行刚性旋转，不做大面积网格拉伸。

### Step 6：建立动画

```text
idle
walk
work
sit
sleep
interact
```

### Step 7：像素修正

对：

```text
手
脚
脸
头发
衣服边缘
武器
```

进行关键帧像素修正。

---

# E.43 为什么不建议“AI 直接生成完整动画 Sprite Sheet”作为唯一方案

AI 很容易产生：

```text
脸变
衣服变
武器变
身材变
像素密度变
```

因此 AI 动画应该作为：

```text
参考
原型
动作草图
```

而不是直接成为长期的唯一 Animation Source。

更稳定的方式：

```text
AI Character Sheet
        ↓
固定角色模板
        ↓
Rig
        ↓
动画
        ↓
Pixel Correction
```

---

# E.44 NPC 批量生成策略

不要每个 NPC 从零开始。

建立：

```text
BodySet
HairSet
FaceSet
ClothingSet
AccessorySet
EquipmentSet
```

然后 Definition：

```json
{
  "id": "villager_worker",
  "body": "body_medium_01",
  "hair": ["hair_01", "hair_02", "hair_05"],
  "clothing": ["shirt_01", "shirt_03", "apron_02"],
  "palette": "village_warm",
  "allowed_jobs": ["farmer", "builder", "worker"]
}
```

NPC Instance 只保存：

```text
body_id
hair_id
face_id
clothing_id
palette_id
```

这样存档很小，也容易批量生成。

---

# E.45 主角与普通 NPC 的区别

普通 NPC：

```text
共享 Skeleton
共享动作
共享 Body Parts
共享服装模板
```

主角：

```text
独立脸部
独立发型
独立服装
更高动画质量
更多 Frame-by-frame Correction
更多特殊动作
```

这样把资源投入到真正重要的角色上。

---

# E.46 Art QA

所有 AI 生成/人工制作资产进入游戏前必须通过：

```text
□ 固定相机截图检查
□ 像素密度检查
□ 调色板检查
□ 透明边缘检查
□ 锚点检查
□ 尺寸检查
□ Billboard 方向检查
□ Shadow 检查
□ 与地面颜色融合检查
□ 与建筑颜色融合检查
□ 与 NPC 颜色融合检查
□ 手机分辨率检查
```

### E.46.1 绝对不允许

```text
AI 图像白边
背景残留
过度平滑
不同像素尺寸混用
颜色过饱和
同类资产风格漂移
随机资产无法复现
```

---

# E.47 美术资源可以“先全部空着”

V2 技术开发阶段允许：

```text
Grass        → Placeholder
Flower       → Placeholder
Tree         → Placeholder
NPC          → Placeholder
VFX          → Placeholder
Texture      → Placeholder
```

但是必须先建立：

```text
Definition
Folder
Naming
Import Rule
Runtime Interface
```

例如：

```text
GrassTuft Definition
       ↓
Placeholder.png
       ↓
正式 GrassTuft.png
```

替换资产不能修改：

```text
TerrainSystem
BuildingSystem
NPCSystem
NavigationSystem
```

这就是 Data-Driven Design。

---

# E.48 第一批必须真正制作的美术资产

为了验证管线，不需要一次做几百个。

第一批只制作：

```text
Terrain
├── grass
├── soil
└── stone

Vegetation
├── grass_tuft
├── flower
├── bush
├── tree
└── pebble

Building Materials
├── wood
├── stone
└── roof

Objects
├── chest
├── table
├── chair
└── workstation

NPC
├── villager_male
├── villager_female
└── child_optional

VFX
├── fire
├── smoke
└── dust
```

只要这套东西能组成：

```text
草地
→ 建房
→ 放家具
→ NPC 行走
→ NPC 工作
→ 火炉工作
```

整个 Art Pipeline 就已经被验证。

---

# E.49 Art Vertical Slice

第一版美术验收场景：

```text
固定 Camera
      ↓
一片 Grassland
      ↓
一条 Dirt Road
      ↓
一栋 Wood + Stone House
      ↓
一个小 Oak Tree
      ↓
若干 Grass / Flower / Pebble
      ↓
一个 NPC
      ↓
NPC Walk
      ↓
NPC Work
      ↓
Campfire
      ↓
Smoke + Fire VFX
```

要求：

> 玩家第一眼应该感觉它是“同一个世界”。

而不是：

```text
建筑像一个游戏
树像另一个游戏
NPC 像第三个游戏
VFX 又像第四个游戏
```

---

# E.50 最终 Art Pipeline

```text
                    MASTER STYLE SHEET
                             │
                   ┌─────────┴─────────┐
                   ↓                   ↓
              AI Generation       Handcrafted Pixel
                   │                   │
                   └─────────┬─────────┘
                             ↓
                        Pixel Cleanup
                             ↓
                        Palette Lock
                             ↓
                       Asset Definition
                             ↓
                         Asset Library
                             ↓
           ┌─────────────────┼──────────────────┐
           ↓                 ↓                  ↓
        Terrain          Building             NPC/VFX
           │                 │                  │
       Pixel Ground      3D Geometry       Pixel Sprite
       Pixel Scatter     Pixel Material     2D Skeleton
           │                 │             + Corrective Frames
           └─────────────────┼──────────────────┘
                             ↓
                     Godot Runtime
                             ↓
                     Fixed Camera
                             ↓
                  Stylized Lighting
                             ↓
                        Game Image
```

---

# Appendix F：NPC 资产生成提示词模板库

## F.1 通用模板

```text
[character identity],
full-body stylized pixel art RPG character,
cozy medieval fantasy countryside,
fixed orthographic game camera,
consistent small-scale pixel density,
clear readable silhouette,
limited color palette,
hand-crafted pixel clusters,
soft warm highlights,
cool soft shadows,
transparent background,
production game sprite,
no text, no watermark, no realistic photo,
no 3D render, no smooth gradients, no anti-aliasing
```

## F.2 Farmer

```text
friendly medieval fantasy farmer,
linen shirt, simple trousers, work apron,
small leather boots,
wooden tool belt,
slightly sun-tanned face,
soft brown and muted green palette,
cozy countryside pixel RPG character,
```

## F.3 Builder

```text
medieval fantasy village builder,
rolled-up sleeves, sturdy vest,
work gloves, leather belt,
small hammer and tool pouch,
muted brown green cream palette,
cozy hand-crafted pixel RPG style,
```

## F.4 Researcher

```text
medieval fantasy researcher,
long simple robe, small satchel,
book and parchment accessory,
subtle magical ornament,
muted teal cream brown palette,
cozy pixel RPG character,
```

## F.5 Blacksmith

```text
medieval fantasy village blacksmith,
sturdy apron, rolled sleeves,
heavy gloves, leather belt,
short boots,
small hammer accessory,
warm brown charcoal copper palette,
cozy pixel RPG style,
```

---

# Appendix G：动作生成与像素修正模板

动作设计提示：

```text
same character,
same proportions,
same outfit,
same hair,
same palette,
consistent sprite silhouette,
[animation name],
[frame count] distinct animation frames,
fixed orthographic game camera,
no camera movement,
no perspective change,
transparent background
```

示例：

```text
same medieval fantasy villager,
same proportions and outfit,
6-frame walk cycle,
clear weight shift,
subtle arm swing,
subtle body bounce,
feet clearly readable,
fixed orthographic game camera,
transparent background,
hand-crafted pixel art
```

---

# Appendix H：Art 资产工程检查清单

```text
[ ] Camera 已锁定
[ ] Master Palette 已锁定
[ ] Pixel Density 已锁定
[ ] Asset Naming 已锁定
[ ] Asset Definition 已建立
[ ] Placeholder 已准备
[ ] Terrain 材质已建立
[ ] Vegetation Scatter 接口已建立
[ ] Building Material 接口已建立
[ ] VFX Atlas 接口已建立
[ ] NPC Sprite 接口已建立
[ ] NPC Skeleton 已建立
[ ] Idle 已完成
[ ] Walk 已完成
[ ] Work 已完成
[ ] Pixel Correction 流程可用
[ ] Screenshot QA 可用
[ ] 手机分辨率检查通过
```

---

# Appendix I：最终 NPC 技术结论

本项目 NPC 最终不是：

```text
复杂 3D Character
```

而是：

```text
3D World Position
+
2D Pixel Character
+
2D Bone Rig
+
Rigid Sprite Parts
+
Hybrid Animation
+
Pixel Corrective Frames
```

即：

> **真正生活在 3D 世界里的像素人物。**

这与 Terrain、Building、WorldObject、InteractionPoint、NPCSystem 的架构完全兼容。


---

# Appendix K：Art / NPC Roadmap 增量

| ID | Block | 内容 | 状态 |
|---|---|---|---|
| ART-09 | Fixed Camera Art Profile | 锁定相机、像素密度、缩放、Anchor、Import Rules | ⬜ |
| ART-10 | Pixel Asset Library | Terrain / Vegetation / Props / VFX / Characters 目录与 Definition | ⬜ |
| ART-11 | Terrain Scatter | Grass / Flower / Pebble / Bush / Tree / Edge Detail | ⬜ |
| ART-12 | Building Material Library | Wood / Stone / Brick / Roof / Plaster | ⬜ |
| ART-13 | Pixel VFX Library | Fire / Smoke / Dust / Magic / Weather | ⬜ |
| ART-14 | NPC Sprite Pipeline | Character Sheet → Parts → Sprite Atlas | ⬜ |
| ART-15 | NPC Skeleton2D | Bone2D + Rigid Sprite Parts | ⬜ |
| ART-16 | NPC Animation Controller | Idle / Walk / Work / Sit / Sleep / Interact | ⬜ |
| ART-17 | Pixel Corrective Animation | 关键动作逐帧修正 | ⬜ |
| ART-18 | Art QA Scene | 固定 Camera 截图一致性测试 | ⬜ |
| ART-19 | AI Asset Ingest | AI 输出 → Cleanup → Metadata → Approved | ⬜ |

## 推荐实施顺序

```text
ART-09
  ↓
ART-10
  ↓
ART-11 + ART-12
  ↓
ART-14
  ↓
ART-15
  ↓
ART-16
  ↓
ART-13
  ↓
ART-17
  ↓
ART-18
  ↓
ART-19 批量化
```

NPC 骨骼与动画不应阻塞 Terrain / Building 核心开发；在 Placeholder Sprite 下即可先完成 NPC State → Animation Controller 接口。

---

# Appendix J：最终美术技术路线

```text
真实 3D 空间
        ↓
固定正交相机
        ↓
建筑 = 3D Geometry + Pixel Material
        ↓
Terrain = 3D Surface + Pixel Ground
        ↓
植物 = Pixel Billboard / Sprite
        ↓
NPC = Pixel Sprite + 2D Bone Rig
        ↓
VFX = Pixel Sprite Particle
        ↓
统一 Palette
        ↓
统一 Pixel Density
        ↓
Stylized Lighting
        ↓
Pixel-oriented Shadow
        ↓
Selective Pixelation / Color Grade
        ↓
最终 Pixel World
```

> **玩家看到的是像素世界；游戏底层运行的是真实三维空间。**

---

---

# Document End

**当前唯一总纲：`《游戏 V2 技术架构·最新整合版》.md`**
