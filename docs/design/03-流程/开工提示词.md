# 开工提示词

> 直接复制粘贴。Claude Code 会读对应笔记，不需要你解释背景。

---

## 做某个板块

```
读 01-项目/xiansuwd/01-板块/00-目录.md 和目标板块笔记，然后做 <板块ID>
```

例：
```
读 01-项目/xiansuwd/01-板块/00-目录.md 和 ART-10-像素资产库.md，然后做 ART-10
```

---

## 只是想看进度

```
读 01-项目/xiansuwd/01-板块/00-目录.md，告诉我当前站位和下一步
```

---

## 要改架构

```
读 01-项目/xiansuwd/00-架构总纲/V2.2-技术架构总纲.md 的 <章节号>，
对照代码看有没有冲突
```

（改架构才读总纲。日常开发不要读，113KB，浪费上下文。）

---

## 收工

```
按 03-流程/更新方案.md 的收工协议更新
```

我会更新：板块笔记 → 目录 → 开发日志 → 仓库 ROADMAP → 提交。

---

## 重新对齐上下文（换了终端 / 隔了几天）

```
读 01-项目/xiansuwd/01-板块/00-目录.md 和
01-项目/xiansuwd/02-开发日志/游戏开发日志.md 最后两条
```

日志里有「关键决策」和「踩的坑」，比读代码快。

---

## 快速自检基线

```
跑一次 headless 自检，确认基线绿的
```

```bash
D:/privacy/Openclaw/Godot-4.7.2/Godot_v4.7.2-stable_win64.exe \
  --headless --path D:/cozy/cozy-game-v2 --quit-after 4500
```

期望：`exit=0`，无 `ERROR/WARNING`，所有断言 `[OK]`。

---

## 项目固定信息（不用每次说）

| | |
|---|---|
| 仓库 | `D:\cozy\cozy-game-v2`（私有 `KawasumiSaki/cozy-game-v2`） |
| Godot | `D:\privacy\Openclaw\Godot-4.7.2\Godot_v4.7.2-stable_win64.exe` |
| Python | `D:\Anaconda\python.exe` |
| 总纲 | `01-项目/xiansuwd/00-架构总纲/V2.2-技术架构总纲.md` |
| 日志 | `01-项目/xiansuwd/02-开发日志/游戏开发日志.md`（**不是** `06-日常/`） |
